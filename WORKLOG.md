# WORKLOG — إحنا واقفين فين

**الملف ده بيجاوب على سؤال واحد: «إيه المفتوح دلوقتي؟»** — مش «المشروع شغال إزاي»، ده مكانه
`CLAUDE.md` وسكيل `codebase-map`.

**قاعدة التحديث:** أي جلسة تخلص شغل، تحدّث الملف ده قبل ما تسيب — تشيل اللي خلص، وتضيف اللي
فتحته، وتكتب أي قرار مستني صاحب المشروع. الملف ده بيتقري أول كل جلسة.

**آخر تحديث:** ١٨ سبتمبر ٢٠٢٦ (**تبويب مراجعة الفيديوهات** — والترقيم كله بقى قوايم، وأهمية لكل غلطة، وصورة كبيرة جوه الصفحة)

---

## ⚙️ حالة التشغيل

| الإعداد | القيمة | يعني إيه |
|---|---|---|
| `working_hours` | **١٠:٠٠ ← ٠٠:٠٠** | ١٠ صباحًا لحد ١١:٥٩ بالليل. `00:00` مقصودة — التحقق بيرفض `24:00` |
| `working_hours_enabled` | `false` | **مابيوقّفش المواعيد.** ٣ من ٤ مستهلكين بيتجاهلوه (الترحيب، الأك، تنبيه التذاكر). بيتحكم في الرد النصي بره الدوام بس |
| `ai_reply_enabled` | `false` | الرد الآلي مقفول |
| `ai_provider` | `anthropic` | بعد مقارنة فعلية: Claude ٨/٨ مقابل Groq ٧/٨ |
| `phone_request_enabled` | `true` | البوت بيطلب الرقم من المجهولين مرة واحدة |
| `platform_url` | `abdullah-habashy.com` | زرار «كمّل الدرس» في تقرير الطالب. **مفيش خانة ليه في اللوحة** — يتغيّر من `settings` |
| `show_answers_to_student` | `true` لكل الاختبارات الخمسة | الطالب بيشوف تصحيح ورقته لما يفتح الرابط بنفس رقمه. خانة في محرر الاختبار تقفلها لاختبار بعينه |
| `answers_after_close` | `false` | لو اتفعّل، التصحيح مايبانش للطالب غير بعد ما الاختبار يتقفل |
| `shuffle_questions` / `shuffle_options` | `false` | ترتيب عشوائي ثابت لكل طالب. الفروع مابتتخلطش جوه رأسها |
| إشعار الدرجة على تيليجرام | **شغّال** | بيتبعت مرة واحدة بعد التصحيح، مابيتسجّلش في المحادثة ومابيستناش وقت العمل |
| `tafra_enrollment_auto_sync_interval_hours` | **٦ ساعات** | مزامنة الاشتراكات بقت مجدولة. كانت يدوية وقعدت ٣ أيام، وكلّفت ٢٩ تذكرة متحوّلة غلط |
| `quiz_grading_mode` | `instant` | التصحيح بيبدأ لحظة التسليم. حوّله لـ`queued` قبل الامتحانات الكبيرة — الورقة بتتسجّل زي ما هي والكرون بيصحّح كل دقيقة. الإعداد مكاش دقيقة فالتغيير بيسري بعد دقيقة |
| نموذج التصحيح + تجريبه | **للأدمن بس** | التيم العلمي بيبني الأسئلة ويراجع الدرجات، ومايشوفش كارت النموذج ولا التجريب. مقفولة بـ `requireAdminApi` مش بالإخفاء |
| قايمة أوامر البوت | **فاضية للطلاب** | الزرار الأزرق متشال بطلب صاحب المشروع. `/report` و`/science` و`/tech` وكلمة «تقريري» شغالين بالكتابة. للرجوع: سطر واحد في `botManager.js` فوق `setMyCommands`. **استثناء واحد:** محادثة الأدمن ليها قايمة بنطاق `chat` فيها `/daily_report` بس — نطاق منفصل مابيأثرش على القايمة العامة |
| حساب الأدمن على البوت | `forward_chat_id` | الحساب المربوط بـ `/setforward`. رسايله مابتفتحش تذاكر، وهو الوحيد اللي بيشوف زرار «📊 التقرير اليومي» |
| `forwarding_enabled` | **`false`** (٥ سبتمبر ٢٠٢٦) | تحويل كل رسالة جديدة لحساب الأدمن **متقفل بطلب صاحب المشروع**. `forward_chat_id` سايب زي ما هو عن قصد — هوية الأدمن والزرار متعلّقين بيه هو مش بالتحويل، فالقفل مابيأثرش على التقرير. للرجوع: خانة التحويل في الإعدادات |

---

## 🎬 مراجعة الفيديوهات — ميزة جديدة، ١٨ سبتمبر ٢٠٢٦

**منشورة ومتجرّبة.** تبويب جديد: التيم العلمي بيتفرّج على الفيديو المصوَّر ويسجّل كل غلطة
بتوقيتها وصورة شاشتها وتعليق بالعربي، والمونتير بيفتح نفس الشاشة (أو الـPDF) ويعدّل على
بريمير ويعلّم إنها اتصلّحت. قبل كده الكلام ده كان بيتبعت واتساب بصيغة «فيه غلطة في نص
الفيديو تقريبًا».

**الملفات:** `controllers/videoReviews.controller.js` · `routes/videoReviews.routes.js`
(`/api/video-reviews`) · `utils/videoReviewExport.js` · تبويب `video-reviews` في
`dashboard.ejs` · تلات جداول في `schema.sql`.

### الجولة التانية — الترقيم قوايم، وأهمية، وصورة كبيرة (نفس اليوم)

أول صف اتسجّل على الإنتاج كان اسمه **«الباب الثاني - الدرس الثاني» مكتوب بالإيد** في خانة
الاسم — يعني الترقيم نص، مايتفلترش ومايترتبش، وكل واحد هيكتبه بصيغة. اتصلّح بطلب صاحب
المشروع:

| الحقل | بقى |
|---|---|
| الباب | قايمة ١-٥ (`review_videos.chapter`) |
| الدرس | قايمة ١-١٥ (`video_number`) |
| التوقيت | تلات قوايم: ساعة ٠-٥ · دقيقة ٠-٥٩ · ثانية ٠-٥٩ |
| أهمية الغلطة | 🔴 لازم تتصلح · 🟡 الأفضل تتصلح · ⚪ سهلة وتعدي (`severity`، الافتراضي `must`) |
| الكتابة الوحيدة | **اسم الدرس** (+ اسم ملف المونتاج اختياري، اتساب بطلب صاحب المشروع) |

- **الحدود مكرّرة في تلات أماكن** — `schema.sql` (CHECK)، والكنترولر، وقوايم
  `dashboard.ejs`. غيّرت واحدة غيّر التلاتة، وإلا القايمة تعرض خيار القاعدة بترفضه.
- **`UNIQUE (book_id, title)` اتشال** — الاسم بقى اسم الدرس بس، و«مراجعة» بتتكرر بين
  الأبواب. منع التكرار بقى على (كتاب، باب، درس) **بتحذير مش رفض**: الحصة ممكن تكون
  متصوّرة على جزئين.
- **الأهمية الغلط بتترد لـ`must`** مش برسالة خطأ — القايمة مقفولة في الواجهة، والقيمة
  الغريبة معناها طلب مش من الشاشة، والأعلى أأمن من رفض ملاحظة الموظف كتبها.
- **زرار تعديل للفيديو** اتضاف (نفس الفورم بيضيف ويعدّل) — من غيره الصف القديم كان
  هيفضل بلا باب ودرس للأبد. **الصف ده لسه موجود على الإنتاج وبيبان عليه تنبيه في
  القايمة** («⚠️ من غير باب ودرس») — يتظبّط من زرار تعديل.
- **الصورة بقت بعرض الكارت جوه الصفحة** (اتقاس: ١٩٢٠px بتتعرض ٩٥٦×٥٣٩) بدل مصغّرة
  ٢٦٠px. `max-width` مش `width` عشان القصاصة الصغيرة ماتتمطّش وتطلع مهببة، وسقف `70vh`،
  والضغط لسه بيكبّرها ملء الشاشة. التعليق فوق الصورة (`column-reverse`) في الشاشة والـPDF.

### 🐛 أول صورة اترفعت طلعت مكسورة — اتصلّحت نفس اليوم

الصورة كانت بتبان أيقونة مكسورة في الكارت، **والسبب شرطة زيادة**:

| المكان | بيخزّن | بيعرض |
|---|---|---|
| مرفقات التذاكر والصوت | `uploads/x.png` | `attachmentUrl()` بتزوّد `/` |
| صور الاختبارات | `/uploads/x.png` | مباشرة `src="${q.image}"` |
| **مراجعة الفيديوهات (الغلط)** | `/uploads/x.png` | `attachmentUrl()` → `//uploads/x.png` |

`//uploads/x.png` عنوان **بروتوكول-نسبي**: المتصفح بيدوّر على دومين اسمه `uploads`،
فالصورة تفشل **من غير أي خطأ في اللوج ولا في الكونسول**. الملف كان موجود على القرص طول
الوقت (٨٠٨ كيلو) والسيرفر بيخدمه ٢٠٠ عادي.

اتصلّح في تلاتة: `attachmentUrl` بقت بتشيل الشرطات الزيادة (**فايدة لكل الشاشات**، وده
المكان اللي بيمنع تكرار الغلطة)، والرفع الجديد بيخزّن من غير شرطة بادئة، وصف موجود اتظبّط
بـ`UPDATE` مشروط في `schema.sql`.

**الدرس:** الشكلين الاتنين موجودين في المشروع فعلًا — **أي كود جديد بيعرض مرفق لازم يعدّي
على `attachmentUrl`**، ومايفترضش شكل المسار.

### القرارات (متتراجعش فيها من غير ما تسأل)

1. **قايمة فيديوهات مُدارة مش خانة نص** — الملاحظات لازم تتجمّع على نفس الفيديو، والنص
   الحر بيخلي نفس الحصة تتكتب بخمس صيغ. نفس المنطق للكتب (`video_books`، مبذورة بـمندليف
   ومذكرة وشرح): «مندليف» و«المندليف» كانوا هيبقوا كتابين.
2. **الصلاحية = صلاحية الاختبارات بالظبط** (`requireQuizAccessApi`: الأدمن + العلمي +
   الفني). مفيش عمود صلاحية جديد، ومفيش تفرقة بين اللي بيسجّل واللي بيصلّح — أي حد داخل
   يقدر يعمل الاتنين، وتعقيد ده كان هيقفل الباب على المونتير لو حسابه بتيم مختلف.
3. **الحالة تلاتة:** مفتوحة · اتصلّحت · مش غلطة. **التعليق إجباري مع الرفض** (نفس قاعدة
   رفض تظلم الطالب) واختياري مع التصليح.
4. **الحذف الكامل بتأكيد تاني** — الفيديو بيمسح ملاحظاته وصورها معاه (CASCADE)، فالسيرفر
   بيرفض الأول ويقول كام ملاحظة هتضيع (`needs_confirmation`، نفس نمط حذف الاختبار)،
   والأرشفة موجودة للحالة اللي مش عايزة حذف. **الكتاب اللي تحته فيديوهات مايتحذفش خالص**
   (`ON DELETE RESTRICT`) — أرشفة بس.
5. **التوقيت بيتخزّن ثواني (`timecode_seconds`) والعرض `HH:MM:SS`** — ترتيب النص بيحط
   «1:00:00» قبل «9:30» وده بيخلي المونتير يلف على الفيديو مرتين. الساعة بتتعرض حتى لو
   صفر. `formatTimecode` مكرّرة في `dashboard.ejs` باسم `vrTimecode` (الواجهة مابتعملش
   require) — **غيّرت واحدة غيّر التانية**.
6. **الصورة اختيارية** — الغلطة الصوتية مالهاش صورة، وإلزامها كان هيخلي التيم يرفع أي صورة
   عشان يعدّي الشرط.

### النص العربي المخلوط بإنجليزي — `dir="rtl"` مش `auto`

التعليقات فيها مصطلحات (`MnO2`، `Premiere`، أسماء ملفات). `dir="auto"` بيحدّد اتجاه الفقرة
من **أول حرف قوي** فيها، فالتعليق اللي بيبدأ بمصطلح إنجليزي بيتقلب كله لليسار والترقيم يروح
للناحية الغلط. `rtl` + `white-space: pre-wrap` في الشاشة وفي الـPDF، والتوقيت لوحده
`direction: ltr; unicode-bidi: isolate` عشان `00:12:40` ماتتقلبش. **اتجرّب: النص بيرجع من
القاعدة مطابق بالبايت.**

### 🐛 عطلان اتكشفوا بتشغيل الكود مش بفحصه

1. **`inconsistent types deduced for parameter $2`** — نفس البراميتر كان مستخدم مرة كقيمة
   لعمود `varchar` ومرة في مقارنة نصية جوه `CASE`. **الكاست الصريح `$2::text` مابيحلّهاش**؛
   الحل بعت «هل رجعت مفتوحة» براميتر منطقي منفصل.
2. **`column "resolved_by" is of type integer but expression is of type text`** — `CASE WHEN
   ... THEN NULL ELSE $5 END` بيخلي نوع البراميتر يتحدد نصًا. الحل `$5::int`.

الاتنين عدّوا `node --check` عادي. اللي مسكهم سكربت شغّل **كل دالة في الكنترولر** على القاعدة
المحلية بـreq/res مزيفين — ده الفحص اللي يتكرر لأي كنترولر جديد.

### الفحص اللي اتعمل

| الفحص | النتيجة |
|---|---|
| ١٥ مسار بـreq/res مزيفين على قاعدة محلية | كلهم تمام، والرفض بيرجّع ٤٠٠/٤٠٩ بالرسالة الصح |
| الحذف بـCASCADE | الملاحظة اتمسحت مع الفيديو ✅ |
| PDF فعلي بـpuppeteer | ٥٤ كيلو، والصورة متضمّنة base64 |
| فحص واجهة في متصفح (٩ خطوات) | كلها ✅ — من غير تشغيل السيرفر، سيرفر ثابت مؤقت على ٤٩٧٧ |
| فحص `getElementById` | مفيش معرّف جديد ناقص (١٦ إنذار كاذب معروف بس) |
| رسم القالب لتلات أدوار | الأدمن والعلمي شايفين التبويب، موظف المتابعة لأ |

**الخط العربي على السيرفر اتفحص بعد النشر:** الـPDF بيتولّد على لينكس بـ`DejaVuSans`،
والحروف بتتوصّل صح والمصطلحات الإنجليزية بتقع في مكانها — اتأكدنا بلقطة من السيرفر نفسه
مش بالافتراض. والمسارات بترد ٤٠١ من غير جلسة.

---

## 🔴 فحص وصول بوت طفرة كان عالق شهر كامل — اتصلّح ١٦ سبتمبر ٢٠٢٦

**الفحص مات وسط شغله ١٧ أغسطس** (بعد ٨٠ ثانية، عند الطالب ٤٠٠ من ٥٨٦)، والسطر في
`new_bot_reachability_sync_status` فضل `running` **شهر كامل**.

والمشكلة مش إنه وقف — المشكلة إن اللوحة بتقفل الزرار على الحالة دي:

```js
button.disabled = sync.status === 'running';
```

يعني **ماكانش فيه طريقة تعيده من الواجهة خالص**. وفي الشهر ده اتراكم ٣٧١٣ طالب مربوط
ماتفحصوش ولا مرة.

### السبب الجذري

`resetStaleSyncStatuses()` في `server.js` بتصلّح الحالات العالقة عند الإقلاع، والتعليق فوقها
بيشرح المشكلة دي بالحرف. بس كانت بتغطي **تلات جداول بس** — مزامنة الطلاب والاشتراكات
والاختبارات. **فحص الوصول والمزامنة الانتقائية كانوا ناقصين.** الاتنين اتضافوا.

> **أي جدول حالة جديد فيه `status='running'` لازم يتضاف للدالة دي.** العلم في الميموري
> بيتصفّر مع كل تشغيل، والسطر في القاعدة لأ — والنتيجة ميزة بتتقفل من غير ما حد يعرف ليه.

### والفحص نفسه كان بيشطب طلاب موجودين

الكاتش كان **فاضي** بتعليق بيقول "الطالب مش قابل للمراسلة":

```js
} catch (error) {
  // مش قابل للمراسلة فعليًا — متوقّع لجزء من الطلاب، نتجاهله ونكمل
}
```

يعني حد من تليجرام (٤٢٩) أو تقطيعة شبكة أو ٥٠٠ = الطالب اتشطب، **والفحص بيخلص
"completed" زي ما يكون تمام**. اتصلّح:

| الرد من تليجرام | التصرف |
|---|---|
| `chat not found` / `bot was blocked` / `user is deactivated` | مش على البوت فعلًا ✅ |
| `429` | يستنى `retry_after` **من غير ما تتحسب محاولة** — الحد مالوش علاقة بالطالب |
| أي حاجة تانية | إعادة مرتين، وبعدين تتسجّل وتتعدّ لوحدها |

ولو خلص وضيّع حد، التحذير بيتكتب في `error_message` **واللوحة بتعرضه** جنب سطر «آخر
فحص» — من غيره الرقم بيبقى ناقص ومحدش ياخد باله.

### نتيجة التشغيل (١٦ سبتمبر، ١٣ دقيقة)

```
2311 reachable, 1402 not on the bot, 0 transient errors
الإجمالي: 3665 ← 5975
```

**٢٣١١ طالب كانوا موجودين طول الوقت ومحدش يعرف.**

### أرقام البوتين بعد الفحص

| | بوت طفرة | بوت المتابعة |
|---|---|---|
| يقدر يراسلهم | **٥٬٩٧٥** | ١٬٠٤٤ |
| على البوت ده بس | ٥٬٤٢٢ | ٤٩١ |
| **على الاتنين** | **٥٥٣** | **٥٥٣** |

**طلاب المنصة ٢٥٬٠٥٢**، منهم ٧٬٣٥٢ مربوطين بتليجرام (٥٬٩٥٠ يوصلهم بوت طفرة و١٬٤٠٢ لأ)،
و**١٧٬٧٠٠ مش مربوطين خالص**.

ومن الـ٥٬٩٧٥: ٣٬٥١١ بس اتبعتلهم رسالة جماعية قبل كده — يعني **٢٬٤٦٤ عمرهم ما اتبعتلهم حاجة**.

### ⚠️ بوت المتابعة مايقدرش يبدأ محادثة مع حد

٤٬٩٩٤ جهة اتصال جاية من مزامنة المنصة `chat_id` بتاعهم بتاع **بوت طفرة**، وبوت المتابعة
بيرد عليهم `Bad Request: chat not found`. ده قيد من تليجرام (البوت مايبدأش محادثة مع حد
مابدأش معاه) مش حاجة في الكود، ومفيش ميزة تحلّه. الطريقين الوحيدين: توصلهم من بوت طفرة،
أو يدوسوا Start على بوت المتابعة.

**بس ٨٦١ طالب عندهم تذكرة وماكتبوش ولا رسالة** — دول بوت المتابعة يوصلهم عادي، والموظف
يفتح تذكرتهم من صندوق الدعم بالبحث بالاسم أو الرقم ويكتب.

---

## 🟢 وصول الطلاب للدعم اتقسّم حسب الاشتراك — ١٦ سبتمبر ٢٠٢٦

الكيبورد بتلات زراير كان بيروح لكل طالب فتح البوت. التيم العلمي أربع أنفار، وكورس
التأسيس المجاني لوحده فيه ٤٠٣٢ اشتراك.

| الجهة | مين يوصلها |
|---|---|
| 🛠️ الدعم الفني | **أي طالب**، مشترك أو لأ. أعطال المنصة بتحصل لأي حد |
| 🧪 الدعم العلمي | المشتركين في كورس متعلّم `grants_support` بس |
| 👥 تيم المتابعة | نفس القاعدة للزرار، **لكن الأمر شغّال للكل** — دي الحالة الافتراضية لكل تذكرة، ومنعها كانت هتسيب غير المشترك محبوس مع الدعم الفني |

**الأرقام على الإنتاج:** ٢٧٩٢ من ٧٨٢١ طالب في البوت شايفين التلات زراير، والباقي الدعم
الفني بس.

### القرارات المتاخدة (متتراجعش فيها من غير ما تسأل)

1. **الكورسات المحسوبة: الباب الأول (30) والمنهج كاملا (31) والباب التاني (32) بس.**
   مراجعة ليلة الامتحان وكورسات ٢ث القديمة **مش** محسوبة بقرار صاحب المشروع.
2. **طلاب التأسيس بيفضلوا مع تيم المتابعة.** التغيير على الزراير بس — تذاكرهم مابتتحوّلش
   لموظف الواتساب. ١٩٣٠ طالب، وموظف الواتساب واحد وماسك ٦١٣ تذكرة بالفعل.
3. **التعليم من اللوحة مش من الكود** — تبويب الإعدادات ← «وصول الطلاب للدعم».

### ⚠️ تعريفان لـ«مشترك» عن قصد

| الملف | السؤال | القاعدة |
|---|---|---|
| `utils/studentAccess.js` | يستاهل دعم علمي؟ | الكورسات المتعلّمة بس |
| `jobs/whatsappRouting.js` | محتاج إقناع يشترك؟ | أي حساب على المنصة |

**متدمجهمش.** الدمج معناه إن ١٩٣٠ طالب تأسيس يتحوّلوا فجأة لموظف الواتساب الوحيد.

### تغييرات سلوك مهمة

- **`STUDENT_MENU_OPTIONS` بقت دالة** (`studentMenuOptions(chatId)`) لأن الكيبورد بقى بيعتمد
  على الطالب. ٢٥ موضع إرسال بيعملوا `await` دلوقتي. **أي موضع إرسال جديد لازم يمرّر
  `chat_id`** وإلا الطالب هيشوف الزراير الغلط.
- **القايمة بتحدّث نفسها:** تيليجرام بيستبدل الكيبورد القديم بالجديد، فالطالب اللي بيشترك
  بيشوف الزرار التالت مع أول رد آلي بعدها. مفيش خطوة تانية.
- **الدعم الفني بقى بيخترق قفل الواتساب** في `transferGuard`. غير المشترك هو بالظبط اللي
  موظف الواتساب ماسكه، والقاعدة القديمة كانت بتقفل الباب الوحيد المفتوح له. الرجوع بيرجّعه
  للمتابعة والتوجيه التلقائي بيرجّعه للواتساب خلال ٥ دقايق لوحده.
- **الفحص في `requestTeam` مش تجميل.** الكيبورد بيخفي الزرار، لكن `/science` والكتابة
  بالعربي شغالين للكل — الفحص هناك هو الباب اللي بيتقفل فعلًا.

### البذرة في `schema.sql`

الـ`UPDATE` بتاع التلات كورسات متشروط بـ`NOT EXISTS (... WHERE grants_support)`. **من غير
الشرط ده أي `npm run migrate` كان هيرجّع القايمة للتلاتة ويشيل الأبواب الجديدة** اللي
الأدمن علّمها.

---

## 🔴 سطر ربط زرار واحد وقّع اللوحة كلها ١٤ ساعة — اتصلّح ١٦ سبتمبر ٢٠٢٦

**البلاغ اللي وصل:** «تيم الدعم الفني والعلمي مش عارفين يسجلوا حضور».
**الحقيقة:** اللوحة كلها كانت واقعة لكل الموظفين، والحضور هو اللي الناس اشتكت منه.

### السبب

واجهة استيراد الشيت (`68a87b1`) اتنشرت بتلت أجزاء، وصل منها **جزء واحد**: السطر اللي
بيربط الزرار. الزرار نفسه والمربع والخمس دوال كلهم ضاعوا من القالب.

```js
// السطر اللي اتنشر لوحده — الـHTML بتاعه مش موجود
document.getElementById('quiz-import-sheet-btn').addEventListener('click', openImportSheet);
// getElementById بيرجّع null → .addEventListener بترمي TypeError
```

**السطر ده في المستوى الأعلى للسكريبت، على بُعد ١٣٨ سطر من نهايته.** فالرمية قتلت كل اللي
بعده، وآخر حاجة في السكريبت هي كتلة التشغيل الأولي:

```js
} else if (hasTicketsAccess) {
  initTickets();          // ← دي اللي بتنادي initTeamAttendance()
}
if (hasTicketsAccess) initLiveEvents();
```

`initTeamAttendance()` هي **المكان الوحيد** اللي بيربط زرار الحضور. الزرار كان بيترسم عادي
ومش بيعمل حاجة لما يتضغط. ومعاه وقع تحميل التذاكر والبث اللحظي وإحصائيات الأدمن.

### إزاي اتأكد إن السيرفر بريء

قبل ما نوصل للواجهة اتنفى كل احتمال في السيرفر:

| الفحص | النتيجة |
|---|---|
| المسارات موجودة ومحمية | `GET/POST /teams/attendance*` بترد ٤٠١ من غير جلسة ✅ |
| `getTeam('science')` / `getTeam('tech')` | الاتنين `requiresAttendance = true` ✅ |
| الفهرس الجزئي اللي `ON CONFLICT` معتمد عليه | `idx_science_attendance_open` موجود ✅ |
| الـINSERT نفسه | اتجرّب جوه `BEGIN/ROLLBACK` لخمس موظفين — أدرج صف لكل واحد ✅ |
| اللوج | مفيش أي خطأ يخص الحضور ✅ |
| صلاحيات الموظفين | كلهم `can_view_tickets = true` ما عدا وفيق (#63) — ده سبب منفصل بيخصه هو بس |

**اللي حسم التوقيت:** `team_attendance` بيقول آخر تسجيل حضور ناجح **١٥ سبتمبر ١٦:٠٣ UTC**،
و`ls -l` على الملف المنشور بيقول إنه اتغيّر **١٩:٥٣ UTC** — نفس لحظة إعادة تشغيل الخدمة.
مفيش ولا تسجيل واحد بعدها.

### الدرس: `node --check` مابيشوفش ده

ده **المصيدة رقم ١ في `CLAUDE.md` بالظبط** (متغيّر/عنصر مش موجود) في شكل جديد: مش متغيّر
غلط، ده عنصر HTML **اتنسي إدراجه** والسطر اللي بيربطه اتنشر لوحده.

**الفحص اللي بيمسك النوع ده** — كل `getElementById('x').` من غير `?.` لازم يكون `x` في القالب:

```bash
node -e "
const fs = require('fs');
const src = fs.readFileSync('src/views/dashboard.ejs', 'utf8');
const ids = new Set([...src.matchAll(/\bid=[\"']([^\"']+)[\"']/g)].map((m) => m[1]));
const missing = [...new Set([...src.matchAll(/getElementById\(\s*'([^']+)'\s*\)(\??)\./g)]
  .filter((m) => m[2] !== '?' && !ids.has(m[1])).map((m) => m[1]))];
console.log(missing.length ? '❌ ' + missing.join(', ') : 'OK');
"
```

⚠️ **الفحص ده بيطلّع إنذارات كاذبة** لفلاتر الطلاب والمكالمات — معرّفاتها بتتولّد بالجافاسكريبت
وقت التشغيل مش مكتوبة في القالب (`calls-search`، `tafra-*`). القايمة المعروفة دي ١٦ معرّف؛
أي اسم **جديد** بيظهر فيها هو المشكلة.

**والفحص التاني اللي بيثبت النتيجة:** ارسم القالب بـ`ejs.render` لتلات حالات — موظف تيم،
أدمن، وموظف متابعة — وشوف العناصر موجودة فعلًا في الـHTML الخارج.

### ملحوظة مفتوحة

**وفيق (#63) عنده `can_view_tickets = false`.** ده مش من العطل ده — `requireTicketsAccessApi`
هيرفض كل طلباته حتى بعد الإصلاح. لو المفروض يسجّل حضور، لازم الصلاحية تتفتح له من كارت
الموظف في اللوحة.

---

## 🟢 رسالة التصحيح بقت عن المعنى بس — ١٦ سبتمبر ٢٠٢٦

الطالب كان بيشوف في ورقته جملة زي «كتابة "ضهر" بالضاد غلطة إملائية ومش محسوبة عليك».
التعليمات كانت بتطلب دي عن قصد (الطالب اللي شايف درجته كاملة وجنبها كلمة غلط بيفتكر إن
التصحيح غلط)، وصاحب المشروع شالها: الجملة بتلفت نظره لحاجة ماكانش لاحظها.

**قواعد التصحيح ماتغيّرتش** — الإملا والخط وشكل الورقة لسه مش محسوبين. اللي اتغيّر إن
النموذج ممنوع **يتكلم** عنهم في الـ`reason`، لا بالنفي ولا بالإثبات.

اتجرّب على الإنتاج بتلات إجابات فيها أغلاط واضحة («الشمع»، «الضهر»، «الاوكسجين»): التلاتة
لسه `correct` بدرجة كاملة، وولا رسالة فيهم بتذكر الإملا.

**القديم اتساب زي ما هو بقرار صاحب المشروع** — ١٦٩٨ إجابة من ٧٧٣٥ لسه فيها الجملة. مفيش
`UPDATE` اتعمل. (التحليل كان: ١٥٥٠ منها ممكن تتشال جملتها نضيف، ٤١ الرسالة كلها عن الإملا،
٧١ متشابكة مع الشرح — لو الموضوع اترجع فيه تاني.)

---

## 🔴 السؤال الفاضي كان بياخد الدرجة كاملة — اتصلّح ١٥ سبتمبر ٢٠٢٦

**اتكشف لما صاحب المشروع امتحن بنفسه وساب كل الأسئلة فاضية وخد ٦ درجات** (`e08a1b1`).

```js
// الكود القديم
Number(answer.selected_option) === Number(question.correct_option)
```

`Number(null)` بيساوي **صفر**. فالسؤال اللي إجابته الصح هي الخيار الأول
(`correct_option = 0`) كان بيطابق الطالب اللي **ماختارش خالص** — وياخد الدرجة كاملة على
سؤال ساب فيه الفراغ.

**⚠️ دي نفس مصيدة `Number(null) === 0` بتاعة ١١ سبتمبر، بالظبط في الاتجاه المعكوس:**
- **١١ سبتمبر:** في **حفظ المفتاح** — سؤال من غير إجابة صح كان بيتحفظ (أ).
- **١٥ سبتمبر:** في **مقارنة إجابة الطالب** — اللي ماختارش بيطابق (أ).

**أي مقارنة على `selected_option` أو `correct_option` لازم تفحص `null` صراحةً قبل
`Number()`.** الفحص اتعمل على كل مواضع المقارنة في المشروع، ولقينا موضع واحد بس هو
المصاب — الباقي (`quizPublic` و`dashboard.ejs` و`quiz.ejs`) كان بيفحص `null` صح.

### الأثر والتصحيح

**اتقاس قبل الإصلاح: ٨٢ ورقة · ٣٦٥ إجابة · ٣٦٥ درجة زيادة** (متوسط ٤.٤٥ للورقة)، كلها في
اختبار الباب الأول.

**اتصحّحت كلها بموافقة صاحب المشروع، من غير إشعارات.** ٢٥ ورقة بقت صفر. ٥٢ طالب كانوا
أخدوا إشعار تيليجرام بالدرجة القديمة — **مااتبعتلهمش إشعار جديد بقرار صريح**، هيشوفوا الصح
لما يفتحوا الرابط.

**نسخة احتياطية قبل التعديل في `_backup_blank_mcq_20260915`** (٣٦٥ صف: الدرجة القديمة لكل
إجابة ومجموع كل ورقة). للرجوع لو احتاج الأمر.

**التحقق بعد التنفيذ:** صفر إجابات فاضية واخدة درجة · صفر أوراق مجموعها مش مطابق لإجاباتها
· كل الحالات `graded`.

---

## ✅ عطل الرصيد: خلص واتصلّح واتحصّن — ١٥ سبتمبر ٢٠٢٦

**الرصيد اتشحن، والـ٢٣٥ ورقة اتصححت كلها، ووظيفة تنبيه اتضافت عشان ده مايتكرّرش بالسكوت.**
الحالة النهائية: **٥٠٧ ورقة `graded` · صفر `partial` · صفر فشل.**

**التكلفة الفعلية: $15** لـ٣١٣٧ نداء على Opus 5 في ٧.٥ دقيقة. التقدير الأولي كان $29.80 —
القياس طلع النص.

**الأثر اللي كان ضايع على الطلاب: متوسط ~+٩ درجة من ٤٥** (٢٠٪ من الدرجة). أمثلة حقيقية:
أحمد خليل ٣٠ ← ٤٣.٨٠ · محمد حسن ٢٤ ← ٣٥.٧٥ · Jana Wael ١٤ ← ٢٤.٠٢.

**الإشعارات ماتبعتتش بقرار صاحب المشروع**، والـ٩٨ ورقة اللي مأخدتش إشعار مفيش فيها ولا طالب
مربوط بتيليجرام — فالقرار اتحقق تلقائيًا.

**الطريقة الصح لو اتكرر:**
```sql
UPDATE quiz_attempts SET grading_status = 'regrading', grading_error = NULL
WHERE grading_status = 'partial';
```
**مش بزرار «أعد تصحيح كل المحاولات»** — ده بيصفّر كل الإجابات المقالية في الاختبار كله
فبيضاعف التكلفة بلا لازمة.

**تصحيح لفهم غلط:** الاعتماد اليدوي (`graded_by = 'staff'`) **محمي فعلًا** من إعادة
التصحيح — `quizScoring.js` بيتخطّاه. التحذير القديم «بتعدّي على المعتمد» معناه **بتتخطاه**
مش بتكتب فوقه.

**التحصين:** `src/jobs/quizGradingAlert.js` — كل ساعة، بتنبّه لو فيه أوراق `partial` عليها
`grading_error` عمرها أكتر من ربع ساعة. **بتروح لـ`forward_chat_id` قبل أي حد** لأن حساب
الأدمن في `users` مش مربوط بتيليجرام. بتترجم الخطأ وتقول اعمل إيه (٧ حالات)، ومابتكررش قبل
٦ ساعات.

**اللي لسه مطلوب منك: سقف شهري في Billing** — لسه مش متظبّط، وده اللي خلّى الرصيد يخلص
بالسكوت.

<details>
<summary>الوصف الأصلي للعطل (للسجل)</summary>

اتكشف في لوج الإنتاج أثناء نشر مرفقات PDF، ومالوش علاقة بأي كود — الرصيد خلص بس:

```
400 invalid_request_error: Your credit balance is too low to access the Anthropic API
```

**الأثر المقاس على الإنتاج:**
- **٢٣١ ورقة `grading_status = 'partial'`** — اتصححت جزئيًا بس، أقدمها **من ١٢ سبتمبر**
  (تلات أيام). الاختيار من متعدد اتصحّح والمقالي لأ.
- **٣١٩٧ إجابة مقالية من غير `graded_at`.**
- كل الـ٢٣١ ورقة `grading_error` بتاعتها نفس الخطأ — يعني سبب واحد مش أعطال متفرقة.

**الطلاب دول سلّموا وشايفين درجة ناقصة**، ومفيش حاجة في الكود هتصلّح ده.

</details>

---


## 🧮 تصحيح محلي من غير رصيد API — ١٧ سبتمبر ٢٠٢٦

**متبني ومتجرّب من الطرف للطرف** (`9e1d41d` · `abb09ed`). `tools/local-grader/` — اقرا
`README.md` اللي جواها.

رصيد الـAPI خلص تلات مرات في نص التصحيح، والورقة بتكلّف ~$0.18. الأداة بتصحّح نفس
الإجابات من جهاز صاحب المشروع بـ`claude -p` (اشتراك Claude Code)، فرصيد الـAPI مابيتلمسش.

**تلات خطوات منفصلة:** `pull` (ينزّل الإجابات وصورها) ← `grade` (يصحّح على دفعات) ←
`push` (يرفع). `push` افتراضيه BEGIN/ROLLBACK ومحتاج `--apply` عشان يكتب فعلًا.

### أرقام متقاسة على إجابات حقيقية (بسعر القايمة)

| الطريقة | لكل سؤال | الزمن |
|---|---|---|
| الـAPI | $0.014 | ~٣ث |
| `claude -p` سؤال لوحده | $0.265 | ٦٥ث |
| `claude -p` دفعة ٨ | $0.0375 | ١٤ث |

كل نداء بيحمّل تعليمات Claude Code وأدواته: حمل ثابت ~$0.25، وزيادة حقيقية ~$0.018
للسؤال. **علشان كده الدفعات.** على الاشتراك الرقم ده مش فلوس — بيتحسب من حد الاستخدام،
فالأداة بتستنى الحد ١٥ دقيقة وتكمّل من مكانها لحد ٨ مرات.

### قرارات

- **قواعد التصحيح اتنقلت لـ`src/utils/quizGradingRules.js`** — وحدة نقية مالهاش أي
  اعتماد على القاعدة، بيقرا منها مسار الـAPI والأداة المحلية. **متكتبش قاعدة تصحيح في
  `tools/`**: المعيار لازم يفضل واحد، وإلا طالب يتحاسب غير زميله ومحدش ياخد باله.
- **الربط بالرقم اللي في رد النموذج مش بالترتيب** — لو قلب الترتيب أو نقص عنصر، المطابقة
  بالموضع كانت هتدّي درجة طالب لطالب تاني.
- **درجات الموظفين مستثناة** (`graded_by <> 'staff'`) في السحب والرفع.
- **أعمدة التوكنز بتتساب NULL** — `costSql` بيحسب منها، والصفر هنا صح: الشغل اتدفع من
  الاشتراك مش من رصيد الـAPI.
- **مساحة الشغل بره المستودع** (`~/.quiz-local-grader`): جوّاها صور إجابات طلبة حقيقيين،
  وكمان `claude` بيحمّل `CLAUDE.md` من مجلد التشغيل وطالع فكان هيضيّع توكنز.

### جودة التصحيح

اتقارن بحكم الـAPI على نفس ٨ إجابات مصوّرة: **٧ متطابقين تمامًا**، والتامن فرق ربع درجة
(`partial 0.75` مقابل `correct 1.00`) والاتنين حكم مدافَع عنه.

### جودة التصحيح — اتقاست على ٩ أوراق كاملة (١٧ سبتمبر)

١٣٥ سؤال، منهم ٥٠ فاضي (صفر تلقائي بلا نداء) و**٨٥ صحّحهم النموذج**. مقابل تصحيح الـAPI:

| | |
|---|---|
| نفس الدرجة بالظبط | **٦٥٪** |
| نفس الحكم | ٧٩٪ |
| أكبر فرق في سؤال | ٠٫٥ درجة |
| صافي الفرق | +٣٫٦٥ على ٩ أوراق (+٠٫٤١ للورقة من ٤٥) |
| التكلفة | $2.28 بسعر القايمة · **صفر فعليًا** · ١١ دقيقة · صفر فشل |

الاختلاف في **تقدير الجزئي** مش في الصح والغلط، وموزّع على ١٣ سؤال من ١٥ — ومرة أكرم
ومرة أقسى في نفس السؤال. يعني تذبذب النموذج، مش عيب في المسار المحلي.

**⚠️ مقارنة غير مكتملة:** ماقدرناش نفصل «فرق المسار» عن «تذبذب النموذج» — ده محتاج
إعادة تشغيل الـAPI على نفس الأوراق، والرصيد كان خالص وقتها.

### 🔒 قرار: الدرجة الموجودة مابتتلمسش — ١٧ سبتمبر ٢٠٢٦

**بطلب صاحب المشروع، وبعد أرقام المقارنة فوق.** `push.js` بيكتب على الأسئلة اللي من
غير درجة بس (`AND an.awarded_points IS NULL`). ٦٥٪ اتفاق معناها إن ثلث الأسئلة هتتغيّر
درجتها لو أعدنا التصحيح — وده بيخلي طالب ياخد درجة غير زميله اللي كتب نفس الإجابة،
لمجرد إن ورقته اتصححت في دفعة تانية.

`--overwrite` موجود للحالة الوحيدة اللي تستاهل: مرجع إجابة اتصلّح والورق كله محتاج
يتعاد بمعيار واحد. **اتجرّب الحارس على الإنتاج** (BEGIN/ROLLBACK): من غيره ٠ من ٥
اتكتبوا، ومعاه ٥ من ٥.

### لسه محتاج

`GRADER_SSH_HOST` و`GRADER_SSH_KEY` في `.env` (اتحطوا على جهاز صاحب المشروع خلاص).
أكبر تشغيل لحد دلوقتي ٨٥ إجابة في ١١ دقيقة — ماتجرّبتش على ألف.

---


## 📝 استيراد فورم وإعادة تصحيح سؤال ٣٧ — ١٧ سبتمبر ٢٠٢٦

**اتنفّذ بالكامل بالتصحيح المحلي — صفر من رصيد الـAPI.**

### ٢٤ ورقة من Google Forms دخلت المنصة

`tools/local-grader/import-form.js`. الطالب بيدخل برقمه على نفس الرابط ويلاقي ورقته
ودرجته ونموذج الإجابة، والتظلم مفتوح له زي أي طالب. متوسطهم ٣٣٫٧٦ من ٤٥.

**٥ طلبة مادخلوش، وده قرار مقصود مش نسيان** (صاحب المشروع قال يتجاوزهم):

| الطالب | المشكلة |
|---|---|
| عزت حازم محمد | كتب «لا ادري» مكان الرقم |
| كريم علي القفاص | كتب «لا يوجد» |
| مريم أنور شلقامي | كتبت «nea72q» |
| «تتعت» | كتب «0000000» |
| محمد (صف ٢١ في الشيت) | كتب رقم بتاع طالب تاني (احمد حمدي عبد المنعم) |

الرابط بيشتغل بالرقم، فمن غير رقم صحيح مفيش طريقة نوصّلهم نتيجتهم ولا نفرّق بينهم وبين
غيرهم. **لو رجعوا بأرقامهم**: `import-form.js` بيتخطّى اللي اتعمله استيراد قبل كده،
فتشغيله تاني بياخد الجداد بس.

### سؤال ٣٧ اتعاد تصحيحه لـ٧٢٥ ورقة

**السبب:** المرجع كان بادئ بسطر «المركب هو MnO2»، وده **معطى في رأس السؤال نفسه**
(المركب XO₂). السؤال بيطلب حاجتين بس: التركيب الإلكتروني للعنصر في XSO₄، وصيغة مركب
آخر يُستخدم كعامل مؤكسد. النموذج كان بيقرا السطر ده كعنصر مطلوب ويخصم على غيابه.

**٣٣٧ إجابة من ٧٢٥ اتخصم منها على حاجة مش مطلوبة.**

| | قبل | بعد |
|---|---|---|
| صح | ٣٣ | **٢٠٠** |
| جزئي | ٤٧٤ | ٣٠٧ |
| غلط | ٢١٨ | ٢٢٣ |

٣٤٨ طلعوا · ١١٧ نزلوا (١١٤ منهم كان القديم بيكافئهم على نسخ المعطى) · صافي +٨١٫٧٨
درجة · ٣ عدّوا النص · ولا واحد نزل تحته · أقصى تغيير نص درجة.

**والسطر اتشال من المرجع بعد كده** عشان أي إعادة تصحيح جاية تبقى أوضح. الهوية لسه
واضحة من `25Mn2+` و`KMnO4`، والدرجات ماتغيّرتش بالشيل.

**الدرس:** الإجابة المرجعية لازم تبقى **المطلوب من الطالب بس**. أي معطى من رأس السؤال
بيتحط فيها بيتحوّل لشرط، والنموذج بيخصم على غيابه — وده بيعدّي من غير ما حد ياخد باله
لأن الدرجة بتبان معقولة.

---

## ⚖️ تظلّم الطالب على درجة سؤال — ١٥ سبتمبر ٢٠٢٦

**منشور ومتجرّب على الإنتاج** (`fc15493` · `82462f8` · `3392616`).

التصحيح المقالي بنموذج، والنموذج بيغلط. الطالب اللي شايف إجابته صح ودرجتها صفر مكانش قدامه
غير إنه يفتح تذكرة دعم ويشرح — والموظف اللي بيقرا التذكرة مش شايف الورقة ولا السؤال. التظلم
بيربط الشكوى بالسؤال نفسه.

**الطالب:** زرار تحت الدرجة («أتظلم من درجتي» / «عدّل تظلمك (٢)») وزرار على كل سؤال ناقص.
المربع بيعرض **كل** الأسئلة القابلة للتظلم مع checkboxes فبيقدّمهم مرة واحدة.

**الموظف:** عدّاد في جدول النتايج (⚖️ N تظلم)، وبانر فوق الورقة بأزرار **بتنقله للسؤال
وتومّضه**، ونص شكوى الطالب جوه كارت السؤال — بيقراها وهو شايف الإجابة والمرجع قدامه.

### القرارات

- ~~**أي سؤال ناقص، مقالي واختيار.**~~ **اتلغى ١٧ سبتمبر: المقالي المتساب فيه إجابة بس.**
  الاختيار تصحيحه مقارنة نصية مش حكم — لو المفتاح غلط بيتصلّح للورقة كلها مش بتظلم فردي.
  والسؤال الفاضي أخطر: **اتقبل تظلم على سؤال الطالب مساب فيه حاجة أصلًا** وده اللي كشف الفجوة.
  الشرط في `APPEALABLE_SQL`: `q.kind = 'essay'` + نص أو صورة مش فاضيين.
- ~~**السقف ٣ أسئلة للورقة**~~ **اتفتح ١٧ سبتمبر بطلب صاحب المشروع** — `quiz_appeal_limit = 0`
  معناها بلا حدود. الدالة `appealLimit()` بتقرا الصفر والفاضي والسالب كلهم «مفتوح».
  (اللي بقى بيمنع الطوفان هو شرط المقالي-المتساب فوق.)
- **تصحيح الموظف بيقفل التظلم في نفس الاستعلام** — زرار «حسمت» منفصل كان هيخلي الموظف
  يعدّل وينسى يدوس، والطالب يفضل مستني حاجة حصلت.
- **اعتماد الورقة الواحدة بيقفل تظلماتها، واعتماد الاختبار كله لأ** — ده بيمر على مئات
  الأوراق، وقفل خمسين تظلم بضغطة معناه إن خمسين طالب اتقالهم «اتراجع» وماحدش بص.
- **التقديم استبدال مش إضافة** — الطالب بيبعت القايمة كاملة فبيعدّل تظلمه، والفاضية بتسحبه.
  **المحسوم مستثنى:** مايتشالش ومايتعادش، وبيتحسب في السقف.
- **`attempt_key` بقى بيرجع بعد التسليم** — `saveProgress` و`submitAttempt` بيرفضوا الورقة
  المسلّمة بـ409 فالمفتاح مابينفعش يعدّل.

**اتجرّب على الإنتاج:** ٦ حالات رفض، والاستبدال، والسحب، وحماية المحسوم، وقفل التظلم عند
التصحيح — وكل صف اتمسح بعده.

### إضافات ١٧ سبتمبر ٢٠٢٦ — الحسم والسجل

**منشور** (`593b824` · `a56e432` · `bd6cf01` · `5c4c81d` · `a4070e1` · `0069fdb`).

- **الحسم بقى قرار مكتوب.** زرار «✅ وافقت» و«✖️ رفضت» مع خانة تعليق على كل سؤال، بيتخزّنوا
  في `quiz_appeals.decision` و`staff_note` وبيظهروا للطالب تحت سؤاله. **التعليق إجباري مع
  الرفض** — «اتراجع» من غير سبب هي نفسها المشكلة اللي التظلم اتعمل عشانها.
- **الصلاحية للتيم العلمي بس** (`requireAppealAccessApi`) — أضيق من صلاحية الاختبارات عن قصد:
  الدعم الفني بيبني أسئلة ويشوف نتايج، لكن حسم التظلم حكم على صحة إجابة علمية. الإخفاء في
  الواجهة تجميل، المنع الحقيقي في الحارس.
- **فلتر «⚖️ التظلمات» في جدول النتايج** — الورقة المتظلم فيها ممكن تكون في أي مكان في ٧٦٣ صف.
- **عمود التظلم بيفضل بعد الحسم.** كان العدّاد بيتحسب من المفتوح بس، فأول ما يتحسم الورقة
  ترجع تبان كأن محدش اتظلم فيها. دلوقتي `listAttempts` بترجّع الإجمالي والمقبول والمرفوض،
  والعمود بيفرّق **المحسوم بقرار** عن **المحسوم مع اعتماد الورقة**. الفلتر بقى بياخد أي ورقة
  اتظلم فيها حد.
- **🐛 التظلم المقفول باعتماد الورقة كان بيختفي من الورقة خالص.** بانر الورقة بيفلتر
  `status = 'open'` والمربع المحسوم بيفلتر `resolved && decision` — واللي اتقفل بزرار
  «وافق على الورقة كلها» مالوش `decision`، فكان بيقع بره الاتنين. دول ٧ من ٩ تظلمات على
  الإنتاج. بقوا بيتعرضوا في المربع المحسوم مع زراير الرد، و`decideAppeal` بتشتغل عليهم
  عادي (الـ`UPDATE` مافيهوش شرط حالة) فالموظف يقدر يضيف رد للطالب بعد ما يكون قفله.
- **الطالب بيلاقي الرد فوق.** بانر `#qz-appeal-summary` أول ما يفتح الرابط تاني: مستني، ولا
  اتحسم وكام اتقبل وكام اترفض. قبله كان الرد جنب السؤال بس والورقة ١٥ سؤال.
- **مفيش إشعار تليجرام بالتظلم** — بقرار صاحب المشروع. الطالب بيعرف لما يفتح الرابط.
- **توضيح الطالب بقى إجباري، ولكل سؤال لوحده** (١٧ سبتمبر). كان اختياريًا وواحد للتظلم
  كله، يعني الطالب اللي اتظلم من تلات أسئلة بيكتب جملة واحدة تتحط على التلاتة — والموظف
  يقرا نفس الكلام تحت كل سؤال من غير ما يعرف مشكلة كل واحد. والاختياري كان بيوصّل
  «راجع السؤال ده» بس، فالموظف يراجع نفس التصحيح ويرجع بنفس النتيجة والطالب يحس إن
  محدش بص. **الحجة القديمة** («الإجبار بيخلي اللي مش عارف يعبّر يسكت») اتشالت من
  `schema.sql` عشان متتقريش كقرار ساري.
  الفرض في `submitAppeal` مش في القاعدة — الصفوف القديمة (٨ من ٤٣) من غير توضيح.
- **🐛 التظلم مكانش شغّال من رابط الطالب أصلًا.** `renderResult` مكانش بيحفظ `attempt_key` في
  مسار الورقة المسلّمة، فالتقديم بيرجّع «المحاولة مش موجودة». المفتاح بقى بيتحط في أول الدالة
  لكل المسارات.

---

## 📷 تصحيح المقالي بصورة الطالب — ١٥ سبتمبر ٢٠٢٦

**منشور ومتجرّب** (`52a1bf4`). الطالب بيصوّر إجابته من الورقة بدل ما يكتبها، والنموذج بيقرا
الصورة ويصحّح عليها.

**ليه:** الكيمياء إجاباتها معادلات ورسومات مالهاش طريقة تتكتب في `textarea`، والكتابة
بالعربي على الموبايل بتخلي الطالب يختصر إجابته فيخسر درجات مش لأنه مش عارف.

- **بديل للكتابة مش إضافة** — الرفع بيفضّي خانة الكتابة ويخفيها، والشيل بيرجّعها.
- **الضغط في المتصفح** (١٦٠٠ بكسل · ٠.٨٥ → ~٦٠٠ كيلو). الكاميرا بتطلّع ٤–٨ ميجا،
  والـbase64 بيزوّد التلت، والنموذج حده ٥ ميجا.
- **التعليمة بتتغيّر مع الصورة:** «الخط والتنسيق مش محسوبين عليه» — من غيرها النموذج بيعلّق
  على الخط بدل ما يصحّح.
- **⚠️ الصورة بتفضل محلية ومابتروحش للسحابة** — التصحيح بيقراها من القرص. لو التخزين
  السحابي اتفعّل، النقل لازم يحصل **بعد** التصحيح.

**أربع أعطال صامتة اتمنعت:**
1. `gradeEssays` كان بيدّي صفر «ساب السؤال فاضي» لأي إجابة نصها فاضي **قبل** ما يدوّر على
   صورة — اللي صوّر كان هياخد صفر.
2. شريط التقدّم كان بيعدّ المكتوب بس، فاللي صوّر كله يشوف «٠ من ١٥».
3. الصورة مكانتش بترجع في `loadSavedAnswers` فالطالب يفتكرها ضاعت بعد إعادة التحميل.
4. الصورة اللي مقدرناش نقراها **بتسيب السؤال بلا درجة** (الورقة `partial` وتروح للموظف) مش
   بتاخد صفر — `score_ratio: null` بيطلّع `points * null = 0`.

**التحقق:** صورة سؤال حقيقية اتبعتت للنموذج، وقرا منها العربي والمعادلات (وصف سلسلة تفاعلات
FeSO₄ صح)، وردّه اتغيّر تمامًا من غير الصورة.

---

## 🎨 دليل ألوان التصحيح + معاينة بعين الطالب — ١٥ سبتمبر ٢٠٢٦

**الاتنين منشورين** (`3181041` و `392291b`).

**دليل الألوان:** أخضر = صح كامل · أصفر = صح جزئي · أحمر = غلط · رمادي = مستني. في
**الشاشتين**. **اللون من الدرجة مش من `ai_verdict`** — الحكم للمقالي بس، فالاعتماد عليه كان
هيسيب الاختيار من متعدد بلا لون. `gradeTone()` و`verdictOf()` ماشيين على نفس العتبات
واتجرّبوا على الحدود. **لو عدّلت واحدة عدّل التانية.**

**معاينة بعين الطالب:** زرار 👁️ للأدمن بيفتح `/quiz-preview/:attemptId` — نفس `quiz.ejs`
بنفس القيود. **المسلّم بس وبضمانتين:** `attempt_key` بيتصفّر (كان هيدّي الأدمن مفتاح يعدّل
إجابات الطالب) و`can_retry` بيتعطّل (زرار بيمسح إجابات الطالب).

**⚠️ مصيدة EJS:** `typeof preview !== 'undefined'` **مابيشتغلش** — EJS بيحل المتغيّرات من
`locals` فبيرمي `preview is not defined`. الصفحة العامة كانت هتقع بـ٥٠٠ **لكل الطلاب**.
الصح `locals.preview`، وأي متغيّر قالب اختياري لازم يتجرّب بـ`ejs.render` في الحالتين.

---

## 📄 مرفقات PDF في الاتجاهين — ١٥ سبتمبر ٢٠٢٦

**منشورة ومتجرّبة على الإنتاج** (`352107a`). الشغل اللي كان نصّه خلصان ومش متكوميت من
٥ سبتمبر بقى مكتمل.

**الطالب ← الموظف:** الطالب يبعت ورقة امتحان مصوّرة PDF، بتتخزّن وتبان في المحادثة رابط
تنزيل باسمه وحجمه. قبل كده كانت بتتسجّل «📎 ملف (اتطلب منه يبعت مكتوب أو صورة)» **والملف
نفسه بيتسقط**.

**الموظف ← الطالب:** زرار 📄 جنب زرار الصورة في صندوق الرد — مذكرة أو نموذج إجابة.

**الحد ٢٠ ميجا في الاتجاهين وده حد تيليجرام مش حدنا:** `getFile` للبوتات مابيفكّش أكبر من
كده، فملف الطالب الأكبر مش هنقدر ننزّله مهما عملنا. وخلّيناه نفس الرقم خارج عشان الموظف
مايشرحش للطالب حد وهو ماشي على حد تاني.

**تلات حدود ثقة، لأن النوع المعلَن والامتداد الاتنين بيكتبهم اللي بيبعت:**
- الوارد بيتفحص `%PDF-` **على البايتات قبل ما يلمس القرص**
- الصادر بيتفحص على القرص بعد ما multer يكتبه (`isPdfOnDisk`)
- الاسم الأصلي **للعرض بس** — الملف على القرص UUID، والاسم بيتنضّف من `/` و`\` ومحارف
  الـbidi (اسم فيه `\u202E` بيبان مقلوب في خانة التنزيل)

**⚠️ الصورة والملف مرفق واحد بس في الرسالة** — تيليجرام مابيبعتش الاتنين، والنص بيبقى
caption لواحد منهم. اختيار واحد في الواجهة بيفضّي التاني، وmulter بيرفض التانية، والكنترولر
بيفحص برضه.

**اللي اتكسر وقت الشغل واتصلّح:** المسار بقى `upload.fields([image, file])` — ده بيملا
`req.files` و**`req.file` بتفضل فاضية**، والكنترولر كان لسه بيقرا `req.file` في ١٠ مواضع.
يعني **رد الموظف بصورة كان مكسور** في النسخة اللي كانت قاعدة في مساحة العمل. أي شغل جاي
على مرفقات الرد: الحقول بقت `req.files.image[0]` و`req.files.file[0]`.

**وأخطاء multer بقت بترجع رسالة مفهومة.** كانت بتروح للمعالج العام وترجّع ٥٠٠ «حصل خطأ في
السيرفر» — الموظف مش عارف إن ملفه كبير ولا صيغته غلط. مع مرفق بيوصل ٢٠ ميجا، احتمال الوصول
للحد بيزيد.

### 🎤 ومعاينة الرسايل الصوتية كانت فاضية — ٣٢١ رسالة

اتكشفت أثناء نفس الشغل. معاينة آخر رسالة في قايمة التذاكر كانت مكتوبة بالإيد في **٥
استعلامات**، وكلها بتغطي النص والصورة بس. يعني **كل رسالة صوتية بيبعتها موظف، الموظف بيفتح
القايمة ويشوف سطر فاضي** مكان آخر رسالة — مش رسالة خطأ، فراغ.

الصيغة بقت دالة واحدة `messagePreviewSql(alias, { withVoice })` في `tickets.controller.js`.
`withVoice` لـ `support_messages` بس — `voice_path` مش موجود في `incoming_messages` أصلًا
(الطالب مابيبعتش صوت). **العدد على الإنتاج بعد الإصلاح: ٣٢١ ← صفر.**

**أي مرفق جديد جاي بيتضاف في الدالة دي مش في ٥ أماكن.**

### التحقق اللي اتعمل

كله قراءة-فقط على الإنتاج قبل النشر:
- **فرعي الـUNION: ٢١ عمود بنفس الترتيب ونفس الأنواع** (الفحص اللي `CLAUDE.md` بيطلبه)
- تذكرة فيها ١٥٩ رسالة بتتحمّل في **٧.٨ مللي**
- `INSERT` الجديد اتجرّب جوه `BEGIN ... ROLLBACK`
- ملف HTML متسمّي `.pdf` بيترفض، و`../../../etc/passwd` بيتنضّف، والاسم الطويل بيتقص لـ١٢٠

---

## 🔑 مفتاح إجابة (أ) الوهمي في الاختيار من متعدد — ١١ سبتمبر ٢٠٢٦

**خلص: العطل اتصلّح ومنشور (`0657549`)، والدرجات اتصحّحت على الإنتاج بموافقة صاحب المشروع.**
عادل ٤٥/٤٥ · ندى ٣٣.٨٥ · إسراء ١١. الإشعار الجديد وصل ندى وإسراء. **عادل مابيوصلوش أي إشعار**
(`400: chat not found`) — رقم تيليجرام متسجّل على المنصة بس عمره ما داس Start على البوت (مفيش
تذكرة)، فإشعار ٨ سبتمبر ماوصلهوش هو كمان. هيشوف ٤٥ لما يفتح الرابط.

عادل مصطفي جاوب كله صح وأخد
٣٨/٤٥ في «اختبار شامل الباب الاول» (`quiz_id = 14`). السبب: ٧ أسئلة (155 158 159 162 165 167
168) مفتاحها `correct_option = 0` أي (أ)، ومحدش من الـ٣ طلاب اختار (أ) في أي واحد فيهم.

**إزاي حصل:** السؤال اللي مالوش إجابة صح كان بيتحفظ (أ) من غير أي تنبيه — افتراضي السؤال
الجديد كان صفر، والمحرر كان **بيعرض (أ) متعلّم عليها** لما المفتاح `null` (`Number(null) === 0`)،
وفحص السيرفر كان بيعدّي `null` لنفس السبب. دلوقتي المفتاح بيبدأ فاضي والسيرفر بيرفض الحفظ.
الفحص على كل الاختبارات: **الاختبار ده بس المتأثر.**

**التصحيح اتعمل بـ SQL مباشر** (المفاتيح لاختيارات عادل + إعادة حساب أسئلة الاختياري السبعة +
تصفير `result_notified_at` عشان الإشعار يتبعت تاني). **مانفعش بزرار «أعد تصحيح كل المحاولات»:** ورقة عادل **معتمدة** (`graded_by = 'staff'` على الـ٤٥)
وإعادة التصحيح بتعدّي على المعتمد، وكمان الزرار بيعيد تصحيح المقالي بالنموذج من غير لازمة.
المفاتيح: 155←2 · 158←2 · 159←1 · 162←3 · 165←1 · 167←1 · 168←1 (صفر = أ).
**لو حد اعتمد ورقة على مفتاح غلط تاني:** نفس الطريقة — الاعتماد بيقفل الدرجات قدام أي زرار.

---

## 🔲 أرقام أسئلة الاختبار في مربع واحد — ١١ سبتمبر ٢٠٢٦

**منشور.** مربعات التنقّل في الشريط اللاصق فوق الاختبار (`/q/:ref`) كانت شريط أفقي بيتسكرول،
بقت كلها ظاهرة وبتنزل سطور (`flex-wrap`). **التمن:** الشريط بيطول مع عدد الأسئلة — امتحان ٥٠
سؤال على موبايل عرضه ٣٦٠ حوالي ٧ سطور. عشان كده `scroll-margin-top` مابقاش ١٢٠ ثابتة: الـ JS
بيقيس طول الشريط في `--qz-bar-h` (`trackBarHeight` في `quiz.ejs`) عشان السؤال مايستخبّاش وراه.

**⚠️ Cloudflare بيكاش الـ CSS ٤ ساعات عند المتصفح** (`max-age=14400` بيكتب فوق الـ 0 بتاع
express). رابط `quiz.css` بقى عليه `?v=20260911` — **غيّر الرقم مع أي تعديل على `quiz.css`**.
`student-report.css` و`student-self.css` لسه من غير رقم، فأي تعديل عليهم هيتأخر نفس المدة.

---

## 🔢 عدّاد التذاكر اتفتح للموظفين — ٧ سبتمبر ٢٠٢٦

**منشور ومتجرّب على الإنتاج.** الموظف بقى يشوف «التذاكر (N) · محتاجة تدخّلك: M» ورقم الصفحة
من إجمالي الصفحات — من غير فلاتر وبعد أي فلتر، لأن الرقم بيتحسب من نفس شرط الفلترة.

**ده عكس قرار قديم مقصود:** الأرقام ماكانتش بتتبعت في الرد أصلًا (مش متخبّية في الواجهة)
عشان الموظف مايقدرش يقدّر حجم المنصة. اتعكس **بطلب صاحب المشروع** — الموظف محتاج يعرف حجم
شغله وقد إيه الفلتر ضيّق النتيجة. **متشيلهوش تاني من غير سؤال.**

الأرقام من التجربة على الإنتاج (هاجر): **١١٦٨ تذكرة · ٢٤ صفحة · ٤ محتاجة تدخّل**، ومع فلتر
`status=new` بقت ٣٤٥، ومع بحث «محمد» ٣٤٦. الأدمن زي ما هو (٢٣٦٥). زمن النداء ٦٩ مللي.

الواجهة ماتغيرتش إلا في سطر العرض — كانت أصلًا بتتفرّع على **وجود** `data.total` مش على الدور.

---

## 🎓 مساحة تدريب الرد الآلي على شات قديم — ٥ سبتمبر ٢٠٢٦

**منشورة وشغّالة** (تبويب الذكاء الصناعي، أدمن بس). كانت تجربة النموذج بتتم بأسئلة نخترعها في
المساحة التجريبية — يعني بنقيس الأسئلة اللي فكرنا فيها مش اللي الطلاب بيسألوها.

**إزاي تشتغل:** تدوّر على محادثة حقيقية → «شغّل» → البرنامج بيعدّي آخر ٢٤ رسالة نصية من
الطالب على النموذج **كل واحدة لوحدها من غير سياق** (زي الإنتاج بالظبط) → بيعرض رد النموذج
جنب **رد الموظف الحقيقي وقتها** → تقيّم ✅/❌ وتكتب ملاحظة.

**التقييم بيكتب في مكانين، الاتنين بموافقة صريحة:**
- الرد الكويس ← زرار «احفظ في المصدر» بيضيفه لـ`ai_knowledge` (نص قابل للتعديل قبل الحفظ،
  ومحمي من التكرار بـ409). نفس مبدأ الحصاد: النموذج يقترح والأدمن يكتب.
- الملاحظة ← تعليمة مرقّمة في `ai_instructions` لو الخانة متعلّم عليها.

**التعليمات بقت قايمة مش نص واحد.** `settings.ai_general_instructions` **لسه شغّالة زي ما
هي وبتتبعت للنموذج**، والقايمة فوقها — لأن النص الواحد مافيهوش طريقة تعطّل أو تحذف تعليمة
واحدة طلعت بتضر، والتدريب بيولّد تعليمة مع كل تقييم. صفر صفوف في القايمة = نفس السلوك القديم.

**رقمان اتقاسوا على الإنتاج وحكموا التصميم:**
- **النداء الواحد ~٨ ثواني.** فالسقف ٢٤ رسالة بتوازي ٤ (~٤٨ ثانية) — مش ٤٠ بتوازي ٣
  (~١٠٧ ثانية) لأن **Cloudflare بيقطع الطلب عند ١٠٠ ثانية بـ524** والجلسة بتبقى اتحفظت في
  القاعدة والأدمن مايشوفش نتيجة.
- **رسايل الوسائط كانت داخلة كأسئلة.** أول تشغيل تجريبي بعت للنموذج
  `🏷️ ملصق (اتطلب منه يبعت مكتوب أو صورة)`. الفلتر دلوقتي في `aiTraining.js` **ومصدّر**
  عشان عدّاد البحث يعدّ بنفس القاعدة.

**ملاحظة على الاستخدام:** التجربة الأولى كانت على محادثة تحفيزية (الطالب بيقول «هحاول
أذاكر»)، والنموذج رد `no_answer` على التلاتة — وده تصرف صح مش عيب. **اختار محادثة فيها أسئلة
حقيقية** عن الاشتراك أو المنصة عشان الجلسة تفيد.

---

## 🧭 قايمة التلات تيمات تحت كيبورد الطالب — ٥ سبتمبر ٢٠٢٦

**منشورة وشغّالة.** كل طالب دلوقتي شايف تلات زراير ثابتة: «🧪 الدعم العلمي» و«🛠️ الدعم الفني»
و«👥 تيم المتابعة» (`src/bot/studentMenu.js`).

- **دي مش القايمة الزرقا** — دي reply keyboard. قايمة أوامر البوت لسه فاضية للطلاب زي ما هي.
- **بتتبعت مع كل رسالة آلية** (الترحيب، رد `/start`، رد بره المواعيد، رد التحويل، وردود
  الموظفين من اللوحة) مش مرة واحدة عند `/start`. السبب: الكيبورد مابيتبعتش لوحده، ولو اتبعت
  عند `/start` بس كان الـ ١٨٠٠ طالب اللي دخلوا قبل النهاردة مش هيشوفوه أبدًا. تيليجرام بيستبدل
  القديم بالجديد فمفيش تكرار عند الطالب.
- **تيم المتابعة هو الافتراضي**، وزرار المتابعة هو طريق رجوع الطالب بنفسه. اللي مش محوّل
  بياخد رد بيقوله إنه معاهم أصلًا. والسحب من موظف الواتساب **ممنوع** زي قاعدة التحويل بالظبط.
- **الإرجاع بقى مشترك:** زرار الموظف المتخصص في اللوحة وزرار الطالب بينادوا نفس
  `performReturn` في `teams.controller.js` — فلو الطالب اختار تيم ونسي يرجع، المتخصص بيرجّعه
  يدويًا زي الأول من غير أي تغيير. وإرجاع الطالب **بيبعت إشعار للموظف الماسك** لأن التذكرة
  كانت بتختفي من عنده في نص الكلام من غير سبب ظاهر.
- **مشاركة الرقم مابقتش تمسح الكيبورد** — `phoneLink.js` كان بيبعت `remove_keyboard`، بقى
  بيرجّع قايمة الطالب مكانها.

**اتجرّب قبل النشر:** تيليجرام قبل الـ markup فعليًا (`is_persistent` مدعومة)، واستعلام
`performReturn` اتجرّب على الإنتاج جوه `BEGIN ... ROLLBACK` ورجّع الماسك القديم صح.

✅ **خلص ١٥ سبتمبر ٢٠٢٦.** التلات مواضع اللي كانت ناقصة في `src/bot/handlers/message.js`
(رد بره المواعيد، الرد الآلي، رد الوسائط غير المدعومة) بقى عليها `STUDENT_MENU_OPTIONS`،
ومعاهم رسالتين فشل حفظ المرفق كمان — نفس القاعدة: أي رسالة بتوصل الطالب بتاخد الكيبورد.
كانت مستنية شغل الـPDF يخلص، وخلص.

---

## 📊 زرار التقرير اليومي لحساب الأدمن — ٥ سبتمبر ٢٠٢٦

**منشور ومتجرّب على بيانات الإنتاج.** الحساب المربوط بـ `/setforward` (اللي بتتحوّل له كل رسالة
جديدة) بقى **أدمن البوت**:

- **رسايله مابقتش تفتح تذاكر.** قبل كده أي حاجة يكتبها للبوت كانت بتتسجّل كرسالة طالب، تفتح
  تذكرة باسمه وتتوزّع على موظف. `bot/handlers/adminReport.js` ميدلوير **مسجّل قبل `start.js`
  و`message.js`** بيمسك رسايله. الأوامر التانية (`/setforward`) بتعدّي عادي بـ `next()`.
- **زرار «📊 التقرير اليومي»** — كيبورد على محادثته + قايمة أوامر بنطاق `chat` فيها
  `/daily_report`. الاتنين بحكم تيليجرام بيظهروا للمحادثة دي بس، فالقايمة العامة للطلاب فضلت
  فاضية زي ما هي.
- **التقرير** (`utils/dailyReport.js`، منفصل عن الهاندلر عشان يتجرّب بـ `node -e`): أداء كل
  موظف النهاردة (ردود + محادثات + مكالمات + تحديثات الأفكار، واللي مالوش نشاط بيتجمّعوا في سطر
  واحد)، الاشتراكات الجديدة النهاردة لكل باب، وإجمالي الاشتراكات لكل باب. بتوقيت القاهرة
  صراحةً لأن القاعدة UTC.
- **الفوتر بيكتب آخر مزامنة اشتراكات** — الأرقام بتيجي من مزامنة كل ٦ ساعات مش لحظية، ومن غير
  السطر ده اشتراك متأخر في المزامنة بيتقري كأنه مش موجود.

**الأدمن لازم يبعت أي رسالة (أو `/start`) مرة واحدة عشان الكيبورد يظهر** — تيليجرام مابيعرضش
كيبورد إلا مع رسالة. قايمة `/daily_report` متسجّلة بالفعل لمحادثته.

**اللي اتقرر ضمنيًا:** الأدمن مستثنى من قايمة أداء الموظفين (التقرير بيتبعتله هو)، والتقرير
**بالضغط بس** مفيش إرسال تلقائي بالليل. لو اتطلب لاحقًا: وظيفة `node-cron` واحدة بتنادي
`buildDailyReport()` وتبعت على `forward_chat_id`.

---

## 🆕 دفعة تحسينات الاختبارات — ١ سبتمبر ٢٠٢٦

سبع ميزات اتضافت في نفس اليوم. كلها منشورة ومتجرّبة، والتفاصيل في رسايل الكوميت:

| الميزة | فين تلاقيها |
|---|---|
| **إحصائيات الأسئلة** — كل سؤال ونسبة اللي جابوه، مرتّبين من الأصعب، وتوزيع الاختيارات | النتايج ← 📊 إحصائيات الأسئلة |
| **مين ماحلّش** — المشتركين في الباب اللي مادخلوش، بأرقامهم | النتايج ← 🔎 مين ماحلّش (لازم تربط الاختبار بباب من المحرر الأول) |
| **تصدير Excel** — شيت درجات + شيت إجابات | النتايج ← ⬇️ تصدير Excel |
| **إشعار الدرجة على تيليجرام** | تلقائي بعد التصحيح |
| **إعادة فتح المحاولة** — لطالب سلّم بالغلط أو النت قطع عليه | النتايج ← افتح المحاولة ← "اسمحله يعيد" |
| **ترتيب عشوائي + تأجيل التصحيح للقفل** | محرر الاختبار، تحت خانة "الطالب يشوف تصحيح ورقته" |
| **استيراد أسئلة من ملف Word** (`س1` / `ج1` / `د1`) | محرر الاختبار ← 📄 استيراد الأسئلة من ملف Word |

**مكتبة جديدة:** `mammoth` (قراءة .docx). أي نشر جاي على سيرفر نضيف لازم يشغّل
`npm install` مش نسخ ملفات بس.

---

## 🔎 طلاب بيدخلوا البوت وهم مش مشتركين — الفحص والنتيجة (١ سبتمبر ٢٠٢٦)

من **١٨٢٨** دخلوا البوت: **١٦٣٠ مشتركين**، و**١٩٨ لأ** (~١١٪) — ١٢٢ مالهمش حساب على
المنصة أصلًا، و٧٦ عندهم حساب من غير اشتراك. و١٩٥ تذكرة محوّلة لتيم الواتساب دلوقتي.

**السبب الأول طبيعي:** رابط البوت عام، والطالب بيسأل قبل ما يشترك. `whatsappRouting`
مصمم للحالة دي وبيشتغل صح.

**السبب التاني كان عطل واتصلّح:** مزامنة الاشتراكات مكانتش مجدولة خالص — بتتشغّل بإيد
الموظف بس، وآخر مرة كانت من ٣ أيام. بقت كل ٦ ساعات
(`tafra_enrollment_auto_sync_interval_hours`).

**وأول مزامنة بعد الإصلاح قاست حجم المشكلة:** ٨٤ اشتراك جديد ماكانوش عندنا، و**٢٩ تذكرة
رجعت لوحدها** من موظف الاشتراكات لفريق المتابعة (١٩٥ ← ١٦٦). يعني ٢٩ طالب دافع كانوا
متعاملين على إنهم لسه مش مشتركين. الرقم ده هو تكلفة ٣ أيام تأخير — ولو المزامنة قعدت
أسبوع كان بقى أكبر.

**لسه مفتوح من نفس الفحص:**
- **٢٦ من الـ١٢٢ المجهولين عندنا رقمهم والرقم على المنصة فعلًا** — دول ممكن يتربطوا
  دلوقتي. الربط بيتم على المنصة (الطالب بيسجّل تيليجرام هناك)، مش عندنا.
- **٤٤ من الـ١٢٢ داسوا Start وماكتبوش ولا رسالة** — وطلب الرقم بيتبعت مع أول رسالة عادية
  مش مع `/start`، فمحدش سألهم. (`bot/handlers/phoneLink.js`)
- و٣٢ بعتوا رسايل قبل ٣٠ أغسطس، يوم ما ميزة طلب الرقم نزلت — الميزة مش بأثر رجعي.
  **اللي بعت رسالة بعد نزولها اتسأل كله**، فالميزة نفسها شغّالة صح.

---

## 📈 التجهيز لمية ألف طالب — ١ سبتمبر ٢٠٢٦

صاحب المشروع بينقل الكود لموقع بمية ألف طالب. اللي اتعمل، كله متقاس مش متوقّع:

| المشكلة | القياس | الحل |
|---|---|---|
| مطابقة تليفون الطالب عند الدخول | Seq Scan · **١٢٥.٧ مللي** على ٢٤٦٠٤ طالب | فهرس على نفس التعبير → **٠.١٠٧ مللي** |
| `quiz_attempts.attempt_key` | **مافيش فهرس خالص** — وهو أكتر عمود بيتبحث بيه | فهرس UNIQUE |
| التصدير بيتبني في الرام | نص مليون صف = أكتر من جيجا | streaming → **٠.١ ميجا لـ٦٠٠٠ محاولة** |
| السؤال عن الدرجة | ٣٠ نداء لكل طالب | تباعد متزايد → **١٠ نداءات**، وأول رد في ثانيتين بدل ٤ |
| عدد الأوراق المتوازية | متغيّر بيئة — محتاج SSH وrestart | إعداد في اللوحة (`quiz_grading_concurrency`، ١–٣٢) |
| عملية واحدة بتعمل كل حاجة | التوسّع مستحيل: نسختين = جدولة مكررة | `APP_ROLE=web/jobs/all` — الافتراضي `all` فمفيش تغيير على النشر الحالي |

**لسه مفتوح ومحتاج قرار:**
- **الصور على القرص المحلي** — مفيش Object Storage. على ١٠٠ ألف طالب القرص بيمتلي
  ويمنع النسخ الاحتياطي. محتاج اختيار مزوّد (S3 / R2 / Spaces) وبيانات دخول.
- **إيميل اختياري يتبعت عليه PDF للتصحيح** (طلب صاحب المشروع) — puppeteer موجود
  فالـPDF مش مشكلة، لكن **مفيش أي إرسال إيميل في المشروع**: محتاج SMTP أو مزوّد
  (Resend / SES) وبيانات دخول.
- **سرعة التصحيح نفسها**: ٢٠ ألف ورقة × ٥ أسئلة مقالية = ١٠٠ ألف نداء على النموذج،
  بتوازي ٤ ده ~٢٠ ساعة. ده قرار معماري (نموذج أرخص للمقالي، تجميع أسئلة في نداء واحد)
  مش مقبض.

---

## ⏳ مفتوح ومستني قرار صاحب المشروع

- **قاعدة معرفة الذكاء الصناعي فيها ٩ معلومات بس** — منها ٣ تجريبية للحذف. الرد الآلي مايتشغّلش
  قبل ما تتملى. أسرع طريقة: تبويب الذكاء الصناعي ← **استخرج**، بيطلّع معلومات حقيقية من كلام
  الفريق نفسه.
- **محدش متعلّم عليه 🛠️ دعم فني** — زرار التحويل موجود، لكن التحويل بيفشل لأن مفيش حد في التيم
  مسجّل حضور. (العلمي فيه ٤ والواتساب ١.)
- **محادثة متعددة الأدوار للرد الآلي** — مقترحة (٣ أدوار وبعدها يسلّم لموظف) ومحتاجة موافقة.
  من غيرها «اسأل الطالب سنة كام وبعدين وضّحله» مابتكملش.
- **حساب الأدمن مش مربوط بتيليجرام** — كود الربط `137A581F` جاهز: ابعت `/linkstaff 137A581F`
  للبوت. من غيره مايقدرش يستقبل روابط ولا إشعارات تشغيلية.
- **تدوير مفتاحي Anthropic و Groq** — اتكتبوا في محادثة، ومفيش حد أقصى شهري متظبّط في Billing.

## 🧹 شغل صغير خلص يوم ١٥ سبتمبر ٢٠٢٦

- **مربعات نتايج الاختبار كانت بتتفتح بره الشاشة** (`03b964c`). التلات مربعات (الإجابات ·
  الإحصائيات · مين ماحلّش) كلهم في الصفحة **تحت جدول النتايج**، والجدول بيوصل ٥٠٠ صف —
  فالموظف بيدوس الزرار، المحتوى بيتحط فعلًا بس ٥ شاشات تحت، وهو شايف إن الزرار باظ. بقى
  فيه `revealQuizPanel(element)` بتمرّر الشاشة للمربع **قبل** انتظار النداء عشان الضغطة
  ياخد ليها رد فعل فوري. **أي مربع جديد بيتفتح بضغطة تحت جدول لازم يستخدمها.**
- **أمر فحص `dashboard.ejs` في `CLAUDE.md` كان بيفشل على الويندوز** (`4fd404c`). كان بيكتب
  في `/tmp/s.js`، وGit Bash بيحوّل `/tmp` لمسار على القرص الحالي فبيرمي
  `ENOENT: open 'E:\tmp\s.js'`. بقى `os.tmpdir()` وبيفحص القالب مع السكريبت، واتجرّب
  منسوخًا من `CLAUDE.md` نفسه على الويندوز وعلى السيرفر.
- **عملية شبح على السيرفر من ٢٧ يوم اتقتلت.** `bash -c until ! pgrep -f 'node rebuild2.js'`
  في حلقة `sleep 8` أبدية، لأن **`pgrep -f` بيطابق سطر أوامر الـshell بتاعه نفسه** فالشرط
  عمره ما يتحقق. معاها `/tmp/rebuild2.log` (نتيجة عدّ دروس من ١٩ أغسطس، مالهاش لازمة).
  **الدرس:** لعدّ العمليات استخدم `ps -eo pid,cmd | grep -E '[n]ode '` (القوس بيمنع
  المطابقة الذاتية) أو `systemctl show -p MainPID` — `pgrep -f` بيدّي رقم أكبر بواحد دايمًا،
  وقريت بيه «نسختين node شغالين» والحقيقة واحدة.

---

## ✅ تصدير PDF كان مكسور واتصلّح — ١٥ سبتمبر ٢٠٢٦

**اتصلّح بموافقة صاحب المشروع.** الإصلاح تثبيت مكتبات النظام للمتصفح مش تغيير كود:
```bash
npx --yes puppeteer browsers install chrome --install-deps
```

**اتجرّب بعد الإصلاح:** تقرير ٢٥ طالب اتولّد PDF سليم (توقيع `%PDF-`) — ١٩٢ كيلو في
٤.٣ ثانية. القرص ٥.٤ جيجا من ٤٨ فمفيش ضغط مساحة.

⚠️ **ملحوظة للمستقبل:** `npm install` لوحده **مش كفاية** لـpuppeteer على سيرفر نضيف —
المكتبة بتتثبت تمام والمتصفح هو اللي بيفشل عند الإقلاع، وعشان كده العطل عدّى من غير ما حد
ياخد باله. أي نشر على سيرفر جديد لازم يشغّل `--install-deps`.

<details>
<summary>وصف العطل الأصلي (للسجل)</summary>

**مالوش علاقة بأي شغل جديد** — اتكشف بالصدفة وإحنا بنجرّب نولّد صورة اختبار.

```
Failed to launch the browser process: Code: 127
libatk-1.0.so.0: cannot open shared object file
```

`puppeteer` متثبت كمكتبة npm، بس **مكتبات النظام اللي المتصفح محتاجها ناقصة**. يعني
`buildStudentsPdf` في `utils/reportExport.js` بيفشل — زرار **تصدير تقرير الطلاب PDF** في
تبويب طفرة. تصدير Excel شغّال عادي.

**الإصلاح:** `npx puppeteer browsers install chrome --install-deps` أو
`apt-get install -y libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libasound2`

**متعملوش من غير سؤال** — ده تثبيت حزم نظام على الإنتاج، مش نشر كود.

**ومش معروف كان بيفشل من إمتى** — مفيش لوج بيقول إن الميزة اتنادت وفشلت، يعني يا محدش
استخدمها يا كانت بتفشل من يوم ما نزلت.

</details>

---

## 🔧 مفتوح وعلى الجلسة الجاية

- ~~الاختبارات الخمسة مفتوحة وعرض التصحيح شغّال عليهم~~ — **الكلام ده بقى غلط، اتقاس
  يوم ١٥ سبتمبر ٢٠٢٦ والوضع آمن:**

  | الاختبار | أوراق | `is_open` | يشوف التصحيح؟ | مؤجّل للقفل |
  |---|---|---|---|---|
  | **١٤ — اختبار شامل الباب الاول** | **٤٩٦** | ✅ مفتوح | ✅ الخانة مفتوحة | **✅ آه** |
  | ٢ · ٤ · ٥ · ٩ · ١٢ · ١٣ (تجريبية) | ١١ إجمالًا | مفتوحة | مفتوحة | ❌ لأ |

  **الاختبار الحقيقي الوحيد (١٤) محمي فعلًا** بـ`answers_after_close = true`: الطالب شايف
  **درجته** ومش شايف **ورقته**، لحد ما الاختبار يتقفل. ده بالظبط اللي الإعداد موجود عشانه.
  والباقي كله تجريبي (١١ ورقة مجموعهم) ومفيش فيه خطر.

  **ومتقفلوش دلوقتي:** آخر تسليم كان الساعة ١٦:٠٧ يوم ١٥ سبتمبر، **٨٠ ورقة في آخر ٢٤
  ساعة**، **١٦ في آخر ٣ ساعات**، و**٩ محاولات لسه بتتحل**. الاختبار شغّال بقوة، والقفل
  دلوقتي بيمنع اللي محلّش. **القفل هو اللي بيفتح التصحيح للـ٤٩٦ كلهم** — فده قرار توقيت
  مش قرار إعداد: استنى لحد ما التسليم يهدى، وبعدها اقفله ويشوفوا ورقهم.

  **اتكشف بميزة «معاينة بعين الطالب»** في أول استخدام ليها — وده كان الهدف منها بالظبط.
- **كلام المصحّح في الإجابات القديمة لسه بصيغة الغايب** («الطالب ذكر…») — اتصحّحت قبل ما
  الصياغة تتغيّر للمخاطب. ٢١ إجابة مقالية بس في الخمس اختبارات كلها، فزرار **«أعد تصحيح كل
  المحاولات»** في صفحة النتايج بيحدّثها في دقيقة. **بس خد بالك:** إعادة التصحيح بتعيد حساب
  الدرجة، فدرجة الطالب ممكن تتغيّر شوية (درجات الموظف اليدوية مابتتلمسش).

- ~~متابعة مزامنة الطلاب~~ — **خلصت ٣٠ أغسطس ٢٠٢٦**: ٢٤٥/٢٤٥، ٢٤٥١٦ طالب، ٧٠١٥ مربوط، من
  غير أخطاء. أول مزامنة كاملة تنجح من شهور. **لو وقعت تاني: اقرا `error_message` قبل أي حاجة**
  — سببها كان تعارض جلسات (جلسة واحدة بس للحساب)، مش مهلة.
- **النسخ الاحتياطي على نفس القرص** — نسخة خارجية لسه ناقصة.
- **⚠️ قرص جهاز الويندوز (E:) امتلى ١٠٠٪ أثناء الشغل يوم ١ سبتمبر** وقطع ملف مصدري
  لصفر بايت في نص الكتابة (اترجّع من git فورًا). فضي دلوقتي ~٣٢ جيجا بس القرص على
  ٩٤٪ — المساحة دي بتخلص بسرعة والغلطة الجاية ممكن تحصل في وقت أوحش.
- **`users.is_science_team` عمود ميت** بعد التعميم لـ `users.team` — الحذف محتاج موافقة.
- **قايمة حالات التذكرة فيها ٥ خيارات واتنين ليهم معنى** — التذاكر مابتتقفلش أصلًا.

---

## 🚩 قرارات متاخدة — متتراجعش فيها من غير سؤال

- **مافيش رجوع للتذكرة من غير قرار يدوي (١ سبتمبر ٢٠٢٦).** المسارين التلقائيين اتقفلوا:
  - **السكوت نص ساعة** — `idleReturn: false` لكل التيمات، وجدولة `teamAutoReturn`
    مابتشتغلش أصلًا.
  - **الانصراف** — كان بيرجّع كل تذاكر الموظف اللي بيمشي، بقى بيقفل الحضور بس.

  **فضل مسار واحد للرجوع:** زرار الإرجاع (`teams.controller.js` → `returnFromTeam`)،
  والمتخصص وموظف المتابعة الاتنين يقدروا يدوسوه.

  **الأثر المقبول:** التذكرة بتفضل باسم المتخصص حتى لو ساكت أو ماشي. واللي بيخليه مقبول
  إن التحويل مشاركة مش نقل ملكية — التذكرة بتفضل ظاهرة في قايمة موظف المتابعة بلون
  التيم وعنده زرار الإرجاع، يعني مش بتختفي.

  **الاستثناء الوحيد الباقي:** توجيه الواتساب (`jobs/whatsappRouting.js`) — الطالب اللي
  اشترك بيرجع من موظف الاشتراكات لفريق المتابعة تلقائيًا. ده مش تحويل يدوي أصلًا: قاعدة
  حوّلته وقاعدة بترجّعه، وقفل نص القاعدة كان هيخلي المشتركين عالقين عنده للأبد.

- **عرض التصحيح للطالب اتفتح بطلب صاحب المشروع (١ سبتمبر ٢٠٢٦).** كان مقفول في `schema.sql`
  بحجّة إن أول طالب يشوف الإجابات ينشرها لزمايله — والاختبار بيتحل على مدى أيام. القرار
  اتغيّر، والحماية دلوقتي مفتاح لكل اختبار (`show_answers_to_student`) مش قفل عام: الاختبار
  اللي لسه بيتحل تقدر تقفل خانته. **متقفلوش تاني من غير سؤال.**

- **تيم المتابعة التليفونية (رحمة، ياسمين، ريناد، سهير) مايشوفش الرسايل المكتوبة.**
  `can_view_tickets = false` مقصودة، واتجرّب يشوفوا تذاكر الطلاب المسندين لهم للمكالمات
  واتشال. شغلهم مكالمات بس.
- **التذاكر مالهاش هدف القفل** — هدفها المتابعة المستمرة. أي كلام عن «إغلاق التذكرة» مبني على
  فهم غلط.
- **الترتيب في تقرير الطالب بيظهر لأعلى ٢٥٪ بس** — الوسيط الفعلي ٨٦٪، فعرضه للكل معناه إن أغلب
  الطلاب يشوفوا نفسهم تحت المتوسط.
- **مفيش مطابقة طلاب بالاسم** — اتقاست: ٦٪ بيطابقوا طالب واحد و٥٪ بيطابقوا أكتر من واحد
  (يعني احتمال نوري طالب بيانات غيره).
- **الرابط المباشر لدرس على المنصة مش ممكن** — التلات أرقام اللي `/lecture-viewer/` محتاجها
  (سلَج الكورس، رقم الكورس، رقم المحاضرة) مش موجودة في الـ API الإداري. اتقفل الموضوع.

---

## 📌 أرقام تفيد في القرارات

| | |
|---|---|
| طلاب المنصة | **٢٤٥١٦** |
| منهم مربوطين بتيليجرام | **٧٠١٥** (كانوا ٤٥٩٠ قبل إصلاح المزامنة) |
| اللي كلّموا بوت المتابعة | ~١٥٠٠ |
| منهم مجهولين (مش مربوطين بحساب) | **٩٣** (كانوا ٣١٢) — الباقي محتاج يشارك رقمه أو يتربط يدوي |
| وسيط درجات الطلاب | **٨٦٪** — الرقم ده بيحكم منطق الترتيب في التقرير |
| تذاكر مسندة لهاجر / مها | ~٦٥٠ / ~٦٤٧ |
| تذاكر مع موظف واتساب | ~٣١٨ |
