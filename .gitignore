node_modules/
.env
# أي نسخة احتياطية أو محلية من .env — فيها نفس الأسرار (.env.example وحده مسموح ومتتبَّع)
.env.bak*
.env.backup*
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
dist/
coverage/
.runtime/

# صور حقيقية اترفعت من طلاب حقيقيين على البوت — بيانات مستخدمين خاصة، معندهاش مكان في مستودع عام
public/uploads/incoming/
public/uploads/support/

# ملف مرجعي فيه بيانات دخول حقيقية (سيرفر/قاعدة بيانات) — محلي بس، أبدًا في مستودع عام
ACCOUNTS.md

# مساحة شغل أداة التصحيح المحلي — الافتراضي بره المستودع، لكن GRADER_WORK_DIR ممكن
# يوجّهها جوه. جوّاها صور إجابات طلبة حقيقيين، نفس سبب استثناء public/uploads/incoming/
tools/local-grader/work/
