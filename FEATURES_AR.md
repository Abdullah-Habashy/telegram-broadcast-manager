# دليل مميزات Telegram Broadcast Manager

آخر تحديث: 5 أغسطس 2026

## 1. نظرة عامة

Telegram Broadcast Manager هو نظام لإدارة التواصل مع مستخدمي بوت تيليجرام. يجمع بين:

- إدارة جهات الاتصال.
- الإرسال الجماعي والفردي.
- جدولة الرسائل.
- صندوق دعم وتذاكر للطلاب.
- الرد على الطلاب من لوحة التحكم.
- تصنيف المحادثات وتوزيعها على الموظفين.
- تحديد مواعيد متابعة مستقبلية.
- تقارير أساسية عن الإرسال والاستقبال.

الواجهة باللغة العربية وتعمل باتجاه RTL، بينما رسائل Terminal باللغة الإنجليزية لتجنب مشكلات عرض الترميز.

---

## 2. الحسابات وتسجيل الدخول

- إنشاء أكثر من حساب لمستخدمي لوحة التحكم.
- تسجيل الدخول بالبريد الإلكتروني وكلمة المرور.
- تشفير كلمات المرور باستخدام `bcrypt`.
- حفظ جلسات تسجيل الدخول داخل PostgreSQL.
- استمرار الجلسة لمدة أسبوع.
- حماية صفحات لوحة التحكم ومسارات API من الاستخدام دون تسجيل الدخول.
- تسجيل الخروج من لوحة التحكم.
- إغلاق التسجيل العام تلقائيًا بعد إنشاء أول حساب.
- أقدم حساب في المشروع القائم يُرقّى تلقائيًا إلى مدير.
- إنشاء حسابات الفريق من صفحة الإعدادات بواسطة المدير.
- تفعيل الحساب أو تعطيله.
- تغيير كلمة مرور حسابات الموظفين والمديرين الآخرين بواسطة المدير.
- منع المدير من تعطيل حسابه أو خفض صلاحيته بنفسه.

### الأدوار والصلاحيات

- `Admin`: وصول كامل إلى الإحصائيات، جهات الاتصال، الإرسال، القوالب، الإعدادات، وإدارة الفريق.
- `Agent`: وصول إلى صندوق الدعم فقط للرد على الطلاب وإدارة التذاكر والمتابعات.
- فلاتر الموظف مخفية داخل زر رمزي ويمكن فتحها عند الحاجة، ويظهر تنبيه بصري على الزر عند وجود فلتر مفعّل.
- التحقق من صلاحية المدير يتم على مستوى API، وليس بإخفاء عناصر الواجهة فقط.
- كل موظف يستخدم حسابًا مستقلًا ويظهر اسمه على الردود التي أرسلها.

---

## 3. ربط بوت تيليجرام

- حفظ أكثر من بوت من صفحة الإعدادات مع اسم واضح لكل بوت داخل اللوحة.
- فحص كل توكن مع Telegram قبل حفظه، وعرض اسم المستخدم الحقيقي للبوت.
- تشفير جميع التوكنات باستخدام AES-256-GCM قبل تخزينها في قاعدة البيانات.
- تفعيل أي بوت محفوظ بضغطة واحدة، مع الاحتفاظ بالبوت السابق للرجوع إليه.
- تجهيز البوت الجديد وتسجيل Webhook له قبل فصل Webhook البوت السابق؛ إذا فشل التحقق يظل البوت الحالي دون تغيير.
- منع حذف البوت المفعّل قبل تفعيل بوت آخر.
- تغيير البوت لا يحذف جهات الاتصال أو التذاكر أو المحادثات أو أي بيانات في قاعدة البيانات.
- استقبال تحديثات Telegram باستخدام Webhook آمن.
- حماية Webhook باستخدام `X-Telegram-Bot-Api-Secret-Token`.
- دعم رابط HTTPS عام من خلال `PUBLIC_URL`.
- الرابط العام ثابت عبر Cloudflare Named Tunnel: `https://support.arabiccoders.com`.

### متطلبات عمل Webhook

- يجب أن يكون السيرفر قيد التشغيل على الـ VPS (خدمة `telegram-broadcast-manager`).
- يجب أن تكون خدمة `cloudflared-tunnel` شغّالة حتى يبقى الدومين متاحًا.
- `PUBLIC_URL` في `.env` على السيرفر يساوي `https://support.arabiccoders.com` ولا يُغيَّر.
- الرابط ثابت ولا يتغيّر عند إعادة التشغيل، فلا حاجة لإعادة تسجيل Webhook بعد كل Restart.

---

## 4. تسجيل جهات الاتصال

### التسجيل التلقائي

- عند إرسال المستخدم `/start` للبوت يتم تسجيله تلقائيًا.
- تُحفظ البيانات التالية عند توفرها:
  - `chat_id`.
  - اسم مستخدم تيليجرام.
  - الاسم الأول.
  - الاسم الأخير.
  - مصدر التسجيل.
  - تاريخ آخر تواصل.
- إذا كانت جهة الاتصال موجودة مسبقًا يتم تحديث بياناتها.
- إذا أرسل مستخدم رسالة دون `/start` يحاول النظام تسجيله تلقائيًا أيضًا.

### استيراد CSV

- استيراد جهات الاتصال من ملف CSV.
- الحد الأقصى لحجم ملف الاستيراد 5MB.
- العمود الإلزامي هو `chat_id`.
- الأعمدة المدعومة:

```csv
chat_id,username,first_name,last_name,phone
```

- تحديث البيانات الموجودة عند تكرار `chat_id` بدل إنشاء نسخة مكررة.
- تجاهل الصفوف ذات `chat_id` غير الصالح مع عرض عدد الصفوف المتجاهلة.
- يوجد قالب جاهز للتحميل من:

```text
/contacts-template.csv
```

مهم: لا يمكن للبوت مراسلة شخص باستخدام رقم الهاتف أو `username` فقط؛ يلزم `chat_id` صالح، وعادةً يجب أن يكون المستخدم قد بدأ محادثة مع البوت.

### تصدير CSV

- تصدير كل جهات الاتصال إلى CSV.
- يشمل الملف بيانات تيليجرام والهاتف والمصدر وتواريخ التسجيل والتواصل.
- ملف التصدير متوافق مع Excel بترميز UTF-8 BOM لعرض العربية بصورة صحيحة.
- يُصدّر `chat_id` كقيمة نصية كاملة لمنع تحويله إلى Scientific notation أو فقدان أرقام.
- تُصدّر التواريخ بصيغة `YYYY-MM-DD` باليوم فقط دون الساعة، بدل ظهورها كأرقام زمنية كبيرة.
- يمكن إعادة استيراد `chat_id` النصي الناتج من ملف التصدير إلى المشروع.

---

## 5. عرض وتحديد جهات الاتصال

تبويب جهات الاتصال والإرسال الجماعي موحّد لتقليل عدد أقسام القائمة الجانبية. يمكن فلترة الجمهور، تحديد النتائج، ثم إنشاء الرسالة من الصفحة نفسها.

- عرض الاسم والعنوان الفرعي وحالة التذكرة والموظف والتصنيفات.
- عرض تاريخ آخر رسالة أرسلها النظام بنجاح إلى المستخدم.
- عرض تاريخ آخر رسالة استلمها النظام من المستخدم.
- يعرض جدول جهات الاتصال تاريخ آخر إرسال وآخر استقبال باليوم فقط دون الساعة، مع الاحتفاظ بالوقت الكامل داخل قاعدة البيانات للفلاتر والترتيب.
- تحديد مستخدم واحد أو عدة مستخدمين بمربعات اختيار.
- تحديد كل النتائج الظاهرة دفعة واحدة.
- تحديد كل النتائج المفلترة دفعة واحدة لاستخدامها كجمهور للإرسال.
- عرض عداد بعدد المستخدمين المحددين.
- الانتقال مباشرة إلى شاشة الإرسال باستخدام زر «مراسلة المحددين».

---

## 6. فلاتر جهات الاتصال

يدعم جمهور الإرسال فلاتر صندوق الدعم: الحالة، العنوان الفرعي، الأولوية، التصنيف، الموظف، حالة موعد المتابعة، وغير المقروء؛ بالإضافة إلى آخر إرسال وآخر استقبال. يمكن دمجها ثم استخدام زر «تحديد كل النتائج المفلترة» لإرسال رسالة لهذا الجمهور فقط.

يوجد فلتران مستقلان ويمكن استخدامهما معًا.

### آخر رسالة أرسلناها

- أي وقت.
- أقدم من يوم.
- أقدم من 3 أيام.
- أقدم من أسبوع.
- لم نرسل له مطلقًا.

يعتمد هذا الفلتر على آخر رسالة ناجحة خرجت من النظام، سواء كانت ردًا من صندوق الدعم أو رسالة جماعية، ولا يعتمد على محاولات الإرسال الفاشلة.

### آخر رسالة استلمناها

- أي وقت.
- أقدم من يوم.
- أقدم من 3 أيام.
- أقدم من أسبوع.
- لم يرسل لنا مطلقًا.

يعتمد هذا الفلتر على سجل الرسائل الواردة من المستخدمين.

---

## 7. التصنيفات Tags

- إنشاء تصنيفات بأسماء وألوان.
- ربط جهة اتصال بأكثر من تصنيف.
- إزالة التصنيف من جهة الاتصال.
- فلترة جمهور الإرسال الجماعي حسب تصنيف محدد.
- العلاقة بين جهات الاتصال والتصنيفات من نوع Many-to-Many.

---

## 8. الإرسال الجماعي والفردي

يمكن إرسال رسالة إلى:

- كل جهات الاتصال.
- كل جهات الاتصال التابعة لتصنيف معين.
- جهات اتصال محددة يدويًا.
- جميع النتائج الناتجة عن دمج فلاتر صندوق الدعم وجهات الاتصال.

### الرسائل النصية

- إرسال نص يصل إلى 4096 حرفًا عند عدم وجود صورة.
- الإرسال يتم بشكل متتابع مع تأخير 60ms بين المستخدمين.
- السرعة النظرية الحالية تقارب 16 رسالة في الثانية، قبل احتساب زمن استجابة Telegram.

### الرسائل المصورة

- إرفاق صورة اختيارية مع الرسالة.
- الأنواع المدعومة: JPG وPNG.
- الحد الأقصى داخل التطبيق: 10MB.
- الحد الأقصى للنص المصاحب للصورة: 1024 حرفًا.
- حفظ الصورة داخل `public/uploads` لدعم الرسائل المجدولة.

### الإرسال الفوري

- إنشاء عملية الإرسال وإرجاع استجابة الواجهة مباشرة.
- تنفيذ الإرسال في الخلفية.
- تسجيل نتيجة الإرسال لكل جهة اتصال.

### الإرسال المجدول

- تحديد تاريخ ووقت مستقبلي.
- يفحص Scheduler الرسائل المستحقة كل دقيقة.
- يدعم النص والصورة.
- يدعم الجميع أو التصنيف أو المستخدمين المحددين.

### تقارير الإرسال

- حالات العملية:
  - `pending`.
  - `sending`.
  - `completed`.
  - `failed`.
- تسجيل حالة كل مستلم:
  - `pending`.
  - `sent`.
  - `failed`.
- حفظ رسالة الخطأ عند فشل الإرسال.
- حفظ وقت الإرسال الناجح.
- عرض عدد الرسائل الناجحة والفاشلة في سجل العمليات.
- تسجيل الرسالة الجماعية الناجحة داخل محادثة كل مستلم لديه تذكرة دعم.
- ربط الرسالة بسجل المستلم لمنع تكرار ظهورها داخل المحادثة.
- ترحيل الرسائل الجماعية الناجحة القديمة إلى سجل المحادثات عند تشغيل Migration.

---

## 9. قوالب الرسائل

- إنشاء قوالب نصية جاهزة.
- حفظ اسم القالب ومحتواه.
- عرض القوالب الحالية.
- حذف القالب.
- إمكانية ربط عملية إرسال بقالب في قاعدة البيانات.

---

## 10. استقبال الرسائل

- استقبال الرسائل النصية الواردة إلى البوت.
- تسجيل الرسالة في قاعدة البيانات مع وقت الاستلام.
- تحديث بيانات جهة الاتصال وتاريخ آخر تواصل.
- إنشاء تذكرة دعم تلقائيًا للمستخدم عند أول رسالة.
- تحديث التذكرة الحالية بدل إنشاء تذكرة جديدة لكل رسالة.
- زيادة عداد الرسائل غير المقروءة.
- إعادة فتح التذكرة المغلقة كـ«جديدة» عند وصول رسالة جديدة.

- يدعم صندوق الدعم الرسائل النصية وصور JPG التي يرسلها Telegram مع Caption اختياري.
- تُحفظ صورة الطالب وتظهر داخل سجل التذكرة وتُحوّل إلى حساب تيليجرام المرتبط.

---

## 11. صندوق الدعم ونظام التذاكر

### تصميم التذاكر

- تذكرة دائمة واحدة لكل جهة اتصال.
- ترحيل الرسائل القديمة إلى تذاكر عند تشغيل Migration.
- عرض أحدث التذاكر أولًا.
- تحميل 50 تذكرة في الصفحة لدعم أعداد كبيرة من الطلاب.
- عدم تحميل آلاف التذاكر دفعة واحدة.

### قائمة التذاكر

- اسم الطالب.
- العنوان الفرعي للمحادثة بجانب الاسم.
- شارة صغيرة تعرض الاسم الأول للموظف المسؤول، بلون ثابت مختلف لكل موظف، وتظهر أيضًا داخل رأس المحادثة.
- معاينة آخر رسالة.
- اتجاه آخر رسالة: واردة أو صادرة.
- وقت آخر نشاط.
- حالة التذكرة.
- الأولوية.
- عدد الرسائل غير المقروءة.
- موعد المتابعة القادم إن وجد.

### البحث

البحث باستخدام:

- الاسم الأول أو الأخير.
- اسم مستخدم تيليجرام.
- رقم الهاتف.
- `chat_id`.

### حالات التذكرة

- جديدة.
- قيد المتابعة.
- بانتظار الطالب.
- مكتملة.
- مغلقة.

عند إرسال أول رد على تذكرة جديدة تتحول تلقائيًا إلى «قيد المتابعة».

### الأولوية

- عاجلة.
- عادية.
- منخفضة.

### التصنيفات الداخلية للتذاكر

- عام.
- تسجيل.
- مصروفات.
- نتائج.
- المنصة.
- شكوى.
- تقني.

### العناوين الفرعية المرنة

- القيمة الافتراضية لكل محادثة هي «مطلوب المتابعة».
- يظهر العنوان بجانب اسم الطالب في قائمة التذاكر وداخل المحادثة.
- يختار المدير أو الموظف العنوان من قائمة منسدلة بجوار الحالة.
- يستطيع المدير أو الموظف إضافة عنوان جديد من القائمة نفسها.
- العنوان الجديد يصبح متاحًا لجميع مستخدمي لوحة التحكم وفي فلتر الجمهور.

### تعيين الموظفين

- تعيين التذكرة لأي مستخدم مسجل في لوحة التحكم.
- عرض اسم الموظف المسؤول.
- فلترة التذاكر حسب الموظف.
- فلتر للتذاكر غير المعيّنة.
- عند الرد على تذكرة غير معيّنة يتم تعيينها تلقائيًا للموظف الذي رد.
- أول موظف يرسل ردًا يدويًا ناجحًا للعميل يصبح الموظف المسؤول تلقائيًا إذا كانت التذكرة غير معيّنة.
- لا تغيّر ردود الموظفين التالية هذا التعيين تلقائيًا.
- يمكن للمدير أو الموظف تغيير المسؤول يدويًا من قائمة الموظف، ويظل الاختيار اليدوي محفوظًا.
- الرسائل الجماعية والمتابعة التلقائية لا تعيّن موظفًا مسؤولًا للمحادثة.
- عند تشغيل Migration تُربط التذاكر القديمة غير المعيّنة بأول موظف أرسل لها ردًا بشريًا مسجلًا.

#### الإسناد الجماعي (المدير فقط)

شريط أعلى قائمة التذاكر يتيح تحديد عدة تذاكر وإسنادها لموظف واحد دفعة واحدة. طرق التحديد الثلاث تتراكم على بعضها، ولا يمسح أي منها ما سبق تحديده:

- **تحديد كل التذاكر المعروضة:** الصفحة الحالية فقط.
- **تحديد كل النتائج (الفلتر الحالي):** كل التذاكر المطابقة للبحث والفلاتر عبر كل الصفحات، لا الصفحة المعروضة وحدها.
- **تحديد أول عدد:** أول N نتيجة من النتائج المطابقة عبر كل الصفحات، بنفس ترتيب القائمة.

### فلاتر صندوق الدعم

- الحالة.
- العنوان الفرعي المرن للمحادثة، مع إمكانية إضافة عناوين جديدة بواسطة المدير أو الموظف.
- الأولوية.
- التصنيف.
- الموظف.
- غير المقروء فقط.
- حالة موعد المتابعة.
- آخر رسالة أرسلها فريق الدعم: أقدم من يوم أو 3 أيام أو أسبوع.
- آخر رسالة استقبلها البوت من الطالب: أقدم من يوم أو 3 أيام أو أسبوع.
- فلترا آخر إرسال وآخر استقبال مستقلان ويمكن دمجهما معًا ومع باقي الفلاتر.
- يمكن دمج الفلاتر والبحث معًا.

كل محادثة تحمل افتراضيًا العنوان الفرعي «مطلوب المتابعة». يظهر العنوان بجانب اسم الطالب في قائمة التذاكر وداخل المحادثة، ويمكن تغييره أو إضافة عنوان جديد من قائمة بيانات التذكرة.

---

## 12. سجل المحادثة والرد

- عرض الرسائل الواردة والصادرة بترتيب زمني.
- تمييز الرسائل الواردة عن ردود فريق الدعم بصريًا.
- عرض اسم الموظف الذي أرسل الرد.
- تصفير عداد غير المقروء عند فتح التذكرة.
- حماية نصوص المستخدمين باستخدام HTML escaping لمنع تنفيذ كود داخل لوحة التحكم.

### خيارات الرد

- نص فقط.
- صورة فقط.
- صورة مع نص.
- منع الإرسال إذا كان النص والصورة فارغين معًا.
- النص بدون صورة حتى 4096 حرفًا.
- النص المصاحب للصورة حتى 1024 حرفًا.
- صور JPG وPNG حتى 10MB.
- حفظ الصورة وإظهارها داخل سجل المحادثة.
- وصول الرد مباشرة إلى الطالب عبر البوت.
- الرسائل الجماعية الناجحة تظهر داخل سجل محادثة الطالب، مع اسم منشئ عملية الإرسال إن كان متاحًا.
- في الهاتف تتحول المحادثة إلى عرض كامل مع زر رجوع لقائمة التذاكر.
- صندوق الرد ثابت وواضح أسفل الشاشة مع زر إرسال بعرض كامل.
- تفاصيل الحالة والتصنيف والموعد مخفية افتراضيًا على الهاتف ويمكن إظهارها عند الحاجة، لإتاحة مساحة أكبر لسجل الرسائل.
- يستطيع الموظف على الهاتف فتح الحالة وبيانات التذكرة من أيقونة مستقلة، وموعد المتابعة من أيقونة التقويم، ثم إغلاق أي قسم لإعادة المساحة للمحادثة.
- أزرار الرجوع والتفاصيل وإرفاق الصورة والإرسال تتحول إلى رموز صغيرة على الهاتف لتوفير المساحة.

### الرسائل الصادرة المسجلة في المحادثة

يسجل النظام حاليًا داخل المحادثة:

- الرد اليدوي النصي أو المصور من الموظف.
- رسالة تعريف الموظف.
- رسالة المتابعة التلقائية عند حلول الموعد.
- الرسائل الجماعية الفورية والمجدولة بعد نجاح إرسالها إلى المستلم.

لا يسجل حاليًا رسالة الترحيب الناتجة عن `/start` أو رسالة الرد التلقائي العامة. كما لا يستطيع اكتشاف رسالة أُرسلت خارج التطبيق باستخدام أداة أخرى أو استدعاء مستقل لـ Telegram Bot API.

---

## 13. مواعيد المتابعة

- تحديد تاريخ ووقت «التواصل القادم» داخل كل تذكرة.
- حفظ الموعد يدويًا.
- زر «تم التواصل / إلغاء الموعد» لمسح الموعد.
- إبقاء الموعد محفوظًا حتى يتم إنهاؤه يدويًا.
- عرض الموعد في قائمة التذاكر.
- تمييز الموعد المستحق أو المتأخر باللون الأحمر.

### الاختيارات السريعة

- بعد يوم.
- بعد يومين.
- بعد 3 أيام.
- بعد 4 أيام.
- بعد 5 أيام.
- بعد 6 أيام.
- بعد 7 أيام.

الضغط على زر الاختيار السريع يحفظ الموعد مباشرة مع الحفاظ على التوقيت المحلي.

### فلاتر المتابعة

- كل مواعيد المتابعة.
- حان موعد التواصل: الموعد الحالي أو المتأخر.
- متابعات اليوم بتوقيت Africa/Cairo.
- مواعيد قادمة.
- بدون موعد متابعة.

التذاكر المكتملة والمغلقة لا تظهر في فلتر المواعيد المستحقة أو القادمة.

### رسالة المتابعة التلقائية

- يحدد المدير نصًا عامًا قابلًا للتعديل من الإعدادات.
- يمكن تفعيل الإرسال التلقائي أو تعطيله.
- يفحص النظام المواعيد المستحقة كل دقيقة.
- عند حلول الموعد يرسل النص للعميل ويسجله داخل المحادثة ثم يمسح الموعد.
- عند فشل Telegram يعيد جدولة المحاولة بعد 5 دقائق.
- تتم معالجة 100 موعد مستحق في كل دورة لتجنب تحميل عدد ضخم دفعة واحدة.

### تعريف الموظف التلقائي

- يمكن للمدير تفعيل رسالة تعريف تسبق أول رد يدوي للموظف.
- النص قابل للتعديل ويستخدم `{name}` لوضع اسم الموظف المسجل في المنصة.
- لا تتكرر الرسالة لنفس الموظف ونفس الطالب أكثر من مرة في اليوم بتوقيت القاهرة.
- إذا رد موظف مختلف في اليوم نفسه تُرسل رسالة تعريف جديدة باسمه.
- تُسجل رسالة التعريف داخل سجل المحادثة قبل الرد الأصلي.
- الردود التلقائية ومتابعات النظام لا ترسل تعريف موظف.

---

## 14. تحويل الرسائل إلى حساب تيليجرام

- ربط حساب تيليجرام محدد لاستقبال نسخة من الرسائل الواردة.
- إنشاء رمز ربط مؤقت من صفحة الإعدادات.
- إرسال الأمر التالي من الحساب المطلوب:

```text
/setforward SETUP_CODE
```

- يجب تنفيذ الأمر في محادثة خاصة مع البوت.
- التحقق من الرمز بطريقة آمنة.
- تغيير الرمز بعد نجاح الربط لمنع إعادة استخدامه.
- حفظ `chat_id` واسم الحساب المرتبط.
- تفعيل أو تعطيل التحويل من لوحة التحكم.
- الرسالة المحولة تشمل:
  - اسم المرسل.
  - username.
  - chat_id.
  - نص الرسالة.
- لا يعيد النظام تحويل رسائل الحساب المستلم إلى نفسه.

ملاحظة: الرسائل المحولة تظهر داخل محادثة واحدة مع البوت. Telegram Bot API لا يستطيع إنشاء محادثة شخصية مستقلة في حساب الموظف لكل طالب.

---

## 15. الرد التلقائي

- تفعيل أو تعطيل الرد التلقائي.
- كتابة نص الرد من الإعدادات.
- إرسال الرد تلقائيًا عند استقبال رسالة نصية عادية.
- تجاهل الأوامر مثل `/start` داخل معالج الرسائل العادية.
- الرد التلقائي يصل إلى المستخدم، لكنه لا يُضاف حاليًا إلى `support_messages` ولا يظهر داخل سجل المحادثة.

---

## 16. الإحصائيات

- إجمالي جهات الاتصال.
- عدد المتفاعلين خلال آخر أسبوع.
- إجمالي الرسائل الجماعية المرسلة بنجاح.
- إجمالي الرسائل الواردة.
- معدل رد تقريبي:

```text
عدد الرسائل الواردة ÷ عدد الرسائل المرسلة × 100
```

هذا المعدل تقريبي وليس قياسًا دقيقًا لرد كل حملة على حدة.

### سرعة الرد

تبويب فرعي مستقل داخل صندوق الدعم بجوار "متابعة الأداء"، يعرض ملخصًا لكل موظف. يرى المدير كل الموظفين مع صف إجمالي للفريق، ويرى الموظف أرقامه هو فقط.

- **مستنية رد:** عدد التذاكر المسندة للموظف، المفتوحة (غير مكتملة أو مغلقة)، وآخر رسالة فيها من الطالب بلا رد. الرقم مطابق لفلتر "مفتوحة ولم يُرد عليها" في صندوق الدعم.
- **أقدم انتظار:** مدة انتظار أقدم تذكرة من هذه التذاكر.
- **متوسط الرد:** متوسط الوقت بين أول رسالة جديدة من الطالب وأول رد عليها.
- **الوسيط:** نصف الردود كانت أسرع منه. مؤشر أدق من المتوسط لأنه لا يتأثر بالرسائل التي تصل ليلًا ويُرد عليها صباحًا.
- **عدد الردود:** عدد الردود التي حُسب عليها المتوسط.

ملاحظات على طريقة الحساب:

- القياس يتم على "موجات" لا على كل رسالة. إذا أرسل الطالب خمس رسائل متتالية ثم رد الموظف مرة، تُحسب مرة واحدة من أول رسالة في الموجة حتى أول رد.
- مدة الرد تُنسب لمن أرسل الرد فعلًا، لا للموظف المسند له التذكرة.
- رسائل الإرسال الجماعي والرسائل التلقائية (رسالة الترحيب والمتابعة) لا تُحتسب ردودًا.
- فلاتر التاريخ تؤثر على أرقام مدة الرد فقط، أما "مستنية رد" و"أقدم انتظار" فهما حالة اللحظة الحالية.

---

### الفلاتر: زر المسح والحفظ التلقائي

كل شاشة فيها فلاتر لها زر «مسح الفلاتر» يعيدها إلى وضعها الافتراضي: صندوق الدعم، والرسائل المميزة، ومتابعة الأداء، وسرعة الرد، والمتابعة التليفونية، وجهات الاتصال، وطلاب المنصة، وبوت طفرة، والتقارير.

وتُحفظ آخر فلاتر استخدمتها لكل شاشة على حدة في المتصفح، فتعود كما هي بعد التحديث أو إغلاق اللوحة وفتحها من جديد. الحفظ يشمل البحث والقوائم متعددة الاختيار ومربعات الاختيار والتواريخ.

ملاحظات:

- الحفظ محلي في المتصفح، فلكل جهاز ومتصفح فلاتره الخاصة، ولا ينتقل بين المستخدمين.
- الضغط على «مسح الفلاتر» يمسح المحفوظ أيضًا، فلا تعود القيم القديمة بعد التحديث.
- الاسترجاع يتم بعد تحميل قوائم الاختيارات (الموظفون، العناوين الفرعية، الأبواب)، لأن القائمة لا تقبل قيمة لخيار لم يُحمَّل بعد.

---
## 17. قاعدة البيانات

يستخدم المشروع PostgreSQL والجداول الأساسية التالية:

- `users`: مستخدمو لوحة التحكم.
- `session`: جلسات تسجيل الدخول.
- `contacts`: جهات الاتصال.
- `tags`: التصنيفات.
- `contact_tags`: ربط التصنيفات بجهات الاتصال.
- `templates`: قوالب الرسائل.
- `broadcasts`: عمليات الإرسال الجماعي والمجدول.
- `broadcast_recipients`: نتيجة الإرسال لكل مستلم.
- `incoming_messages`: الرسائل النصية والصور الواردة.
- `tickets`: تذاكر الدعم.
- `ticket_subtitles`: العناوين الفرعية المرنة للمحادثات.
- `support_messages`: الردود اليدوية ورسائل المتابعة والتعريف والرسائل الجماعية الظاهرة في المحادثة.
- `agent_contact_introductions`: سجل منع تكرار تعريف الموظف خلال اليوم.
- `settings`: إعدادات البوت والرد والتحويل.
- `bot_profiles`: ملفات البوتات المحفوظة والتوكنات المشفرة وحالة البوت المفعّل.

توجد فهارس مخصصة للبحث في:

- تواريخ آخر تواصل.
- الرسائل الواردة حسب المستخدم والوقت.
- الرسائل الصادرة حسب المستخدم والوقت.
- حالات التذاكر وآخر نشاط.
- الموظف المسؤول.
- مواعيد المتابعة.

---

## 18. التقنيات المستخدمة

- Node.js.
- Express 5.
- Telegraf.
- PostgreSQL.
- EJS.
- JavaScript في الواجهة دون build step.
- node-cron.
- multer لرفع الملفات.
- bcrypt لكلمات المرور.
- express-session وconnect-pg-simple للجلسات.
- csv-parse وcsv-stringify.

---

## 19. الأمان الحالي

- تشفير كلمات المرور.
- تشفير توكن البوت.
- Cookies من نوع `httpOnly`.
- تفعيل Secure cookies في الإنتاج.
- حماية API بتسجيل الدخول.
- Webhook secret token.
- Parameterized SQL queries لتقليل مخاطر SQL Injection.
- تحديد أنواع وأحجام الصور المرفوعة.
- HTML escaping لرسائل الطلاب داخل صندوق الدعم.
- رمز ربط مؤقت لتحويل الرسائل إلى حساب تيليجرام.

---

## 20. حدود وملاحظات تشغيلية

- السيرفر وWebhook يجب أن يظلا يعملان لاستقبال الرسائل.
- Cloudflare Quick Tunnel مؤقت وغير مناسب للإنتاج.
- الصور محفوظة على القرص المحلي؛ الاستضافات ذات القرص المؤقت تحتاج Object Storage مثل S3 أو Cloudflare R2.
- لا يوجد Queue خارجي مثل Redis/BullMQ حتى الآن.
- لا توجد إعادة محاولة تلقائية متقدمة عند أخطاء Telegram أو `429`.
- لا يوجد زر لإيقاف حملة بدأت بالفعل.
- تشغيل حملتين بالتزامن قد يجمع سرعتيهما.
- لا توجد حتى الآن صلاحيات أكثر تفصيلًا داخل دور Agent، مثل السماح بتصنيفات معينة فقط.
- رسالة الترحيب بعد `/start` والرد التلقائي العام لا يظهران حاليًا داخل سجل المحادثة.
- الرسائل المرسلة خارج التطبيق لا يمكن اكتشافها أو إضافتها تلقائيًا إلى سجل المحادثة.
- لا توجد إشعارات متصفح أو Email عند حلول موعد المتابعة؛ الموعد يظهر من خلال الفلتر والقائمة.
- لا يوجد سجل تدقيق Audit Log لتغييرات الموظفين حتى الآن.

---

## 21. التشغيل والنشر

### الوضع الحالي: سيرفر أونلاين برابط ثابت

التطبيق منشور ويعمل بشكل دائم على VPS، والرابط العام ثابت:

```text
https://support.arabiccoders.com
```

الرابط مربوط بالسيرفر عن طريق Cloudflare Named Tunnel، فلا يحتاج فتح أي منفذ على السيرفر ولا يتغيّر عند إعادة التشغيل.

الخدمات على السيرفر تُدار بواسطة `systemd`:

| الخدمة | الوظيفة |
|---|---|
| `telegram-broadcast-manager` | تطبيق Node نفسه (`/root/app`) |
| `cloudflared-tunnel` | التانل الذي يربط الدومين بالسيرفر |
| `postgresql` | قاعدة البيانات |

أوامر الإدارة الشائعة:

```bash
systemctl status telegram-broadcast-manager
systemctl restart telegram-broadcast-manager
journalctl -u telegram-broadcast-manager -n 100 --no-pager
```

### طريقة النشر الحالية

> ⚠️ `/root/app` **ليس** مستودع git. النشر يتم بنسخ الملفات يدويًا من جهاز التطوير إلى السيرفر، ثم إعادة تشغيل الخدمة. لا يوجد `git pull` على السيرفر.

```bash
# من جهاز التطوير
scp -i <path-to-key> -r src/ root@<server-ip>:/root/app/

# على السيرفر عند تغيّر dependencies أو schema
cd /root/app && npm install --omit=dev && npm run migrate
systemctl restart telegram-broadcast-manager
```

الخدمة مُعرَّفة في `/etc/systemd/system/telegram-broadcast-manager.service` مع `Restart=always`، وتكتب مخرجاتها بالإلحاق إلى `/root/app/service-out.log` و `service-err.log`.

**نتيجة مهمة:** لأن النشر يدوي، فالنسخة المنشورة قد تسبق ما هو متكوميت في git. تم التحقق في 2026-08-17 أن ملفات السيرفر مطابقة تمامًا لنسخة العمل المحلية غير المتكوميتة، أي أن الإنتاج **يعمل بكود غير محفوظ في git**. التحقق يتم بمقارنة `md5sum` على السيرفر مع `Get-FileHash` محليًا.

### التشغيل المحلي للتطوير فقط

```powershell
npm install
npm run migrate
npm run dev
```

يفتح التطبيق على `http://localhost:3000`.

> **مهم:** التشغيل المحلي للتطوير وقراءة الواجهات فقط. لا تسجّل Webhook من الجهاز المحلي ولا تضع فيه رابطًا عامًا، لأن Telegram يسمح بـ Webhook واحد فقط للبوت — أي تسجيل محلي **يسحب البوت من السيرفر** ويوقف استقبال الرسائل على الموقع الفعلي.

### ملاحظة تاريخية: Cloudflare Quick Tunnel (متوقف)

كان النظام يعتمد سابقًا على Quick Tunnel يولّد رابط `trycloudflare.com` مؤقتًا عند كل تشغيل، مع سكربت `START_QUICK_TUNNEL.bat` يحدّث `PUBLIC_URL` ويُشعر الحسابات بالرابط الجديد عبر `TUNNEL_NOTIFY_CHAT_IDS`. هذه الطريقة **ألغيت بالكامل** بعد الانتقال إلى السيرفر والرابط الثابت، وحُذفت سكربتاتها من المشروع.

---

## 22. خطة التحسينات المستقبلية

المقترحات التالية محفوظة للتنفيذ لاحقًا، ولا تُعد خصائص منفذة حاليًا إلا إذا نُقلت إلى أقسام المميزات الفعلية في هذا الملف.

### المرحلة الأولى: الاستقرار والأمان

1. ~~نشر التطبيق على استضافة دائمة برابط HTTPS وWebhook ثابت بدل Cloudflare Quick Tunnel.~~ **تم** — الآن على VPS برابط ثابت `https://support.arabiccoders.com` (راجع القسم 21).
2. استخدام Redis وBullMQ أو Queue مماثلة لمعالجة الإرسال الجماعي.
3. التحكم في سرعة الإرسال واحترام `retry_after` وإعادة المحاولة عند أخطاء Telegram و`429`.
4. إضافة إيقاف واستكمال الحملات ومنع التعارض بين حملتين متزامنتين.
5. تسجيل رسالة الترحيب بعد `/start` والرد التلقائي العام داخل سجل محادثة العميل.
6. إضافة نسخ احتياطي تلقائي ودوري لقاعدة البيانات واختبار الاستعادة.
7. إضافة سجل تدقيق Audit Log لتغييرات الحالة والموظف والتصنيف والمواعيد.
8. إضافة Rate limiting لمحاولات تسجيل الدخول وحماية CSRF للنماذج الحساسة.
9. نقل الصور والمرفقات إلى Object Storage مثل S3 أو Cloudflare R2.
10. إضافة مراقبة للأخطاء وحالة Webhook والـQueue والتنبيه عند التوقف.

### المرحلة الثانية: إدارة الموظفين

1. توزيع المحادثات الجديدة تلقائيًا وبالتساوي على الموظفين المتاحين.
2. دعم التوزيع حسب القسم أو التصنيف أو العنوان الفرعي.
3. تحديد حد أقصى للمحادثات المفتوحة لكل موظف.
4. إضافة حالة للموظف: متاح أو مشغول أو غير متصل.
5. عدم تعيين محادثات جديدة لموظف غير متاح.
6. إضافة صلاحيات تفصيلية حسب القسم أو نوع التذكرة.
7. إضافة ملاحظات داخلية لا تصل إلى الطالب مع إمكانية الإشارة إلى موظف آخر.
8. تقارير أداء تشمل عدد المحادثات ومتوسط زمن أول رد وزمن الإغلاق والمتابعات المتأخرة.

### المرحلة الثالثة: تحسين صندوق الدعم

1. البحث داخل نصوص الرسائل القديمة بالتاريخ والموظف ونوع الرسالة.
2. ردود جاهزة داخل صندوق المحادثة مع اختصارات مثل `/fees` و`/register`.
3. دعم متغيرات القوالب مثل `{first_name}` و`{agent_name}`.
4. إجراءات جماعية لتغيير الحالة أو الموظف أو العنوان الفرعي أو موعد المتابعة.
5. دعم ملفات PDF وWord والرسائل الصوتية والفيديو مع معاينة مناسبة.
6. إضافة إشعارات متصفح أو Email للمواعيد والتذاكر الجديدة.
7. إضافة تقارير SLA وتنبيهات عند تجاوز زمن الرد المستهدف.

### المرحلة الرابعة: الحملات والتواصل الذكي

1. استبعاد من تواصل معه الفريق مؤخرًا أو لديه تذكرة مفتوحة أو طلب إيقاف الرسائل.
2. استبعاد جهات الاتصال التي فشل الإرسال إليها عدة مرات.
3. حملات متعددة الخطوات تتوقف تلقائيًا عند رد الطالب.
4. قياس عدد الطلاب الذين ردوا بعد كل حملة ومقارنة أداء القوالب.
5. تصدير تقارير الحملات ونتائج المستلمين إلى Excel.
6. إضافة شاشة تأكيد قبل الإرسال تعرض عدد الجمهور والفلاتر المستخدمة.
7. إضافة حماية تمنع إرسال الحملة نفسها مرتين بالخطأ.

### ترتيب التنفيذ الموصى به

1. الاستضافة الدائمة والنسخ الاحتياطي.
2. Queue وإعادة المحاولة واحترام حدود Telegram.
3. تسجيل جميع الرسائل التي يرسلها المشروع داخل المحادثة.
4. سجل التدقيق والصلاحيات والحماية الإضافية.
5. تقارير أداء الموظفين وSLA.

---

## 23. نقل المشروع إلى بوت قائم وإيقاف البوت الحالي

يمكن توصيل المشروع ببوت آخر يعمل بالفعل مع الحفاظ على قاعدة البيانات وحسابات الموظفين والإعدادات والقوالب والتذاكر وخصائص المشروع. إذا كان الطلاب قد تواصلوا مسبقًا مع البوت الجديد ويوجد `chat_id` لكل طالب، يستطيع البوت الجديد مراسلتهم بعد إتمام النقل.

### البيانات المطلوبة

يُحوّل شيت Excel إلى CSV، ويكون العمود الإلزامي:

```csv
chat_id,username,first_name,last_name,phone
```

يمكن ترك الأعمدة الاختيارية فارغة، لكن يجب أن يكون `chat_id` صحيحًا. عند وجود `chat_id` مسبقًا في قاعدة البيانات تُحدّث بيانات الطالب بدل إنشاء سجل مكرر.

### خطوات النقل

1. أخذ نسخة احتياطية من قاعدة البيانات الحالية وملف `.env`.
2. تصدير شيت الطلاب بصيغة CSV واستيراده من تبويب جهات الاتصال والإرسال.
3. التأكد من عدد السجلات المستوردة والصفوف المتجاهلة.
4. الحصول على Token البوت الجديد من BotFather وعدم مشاركته مع أي شخص.
5. التأكد من إيقاف النظام القديم الذي يستخدم البوت الجديد؛ لأن Telegram يسمح بـWebhook واحد فقط لكل بوت.
6. استبدال توكن البوت الحالي بتوكن البوت الجديد من إعدادات لوحة التحكم.
7. التأكد من صحة `PUBLIC_URL` وتشغيل السيرفر حتى يُسجل Webhook على البوت الجديد.
8. اختبار استقبال رسالة والرد عليها والإرسال الجماعي إلى طالب واحد أو مجموعة صغيرة.
9. بعد نجاح الاختبار، إيقاف استخدام البوت القديم وإلغاء أي Webhook أو خدمة قديمة مرتبطة به.

### ما يظل محفوظًا

- قاعدة البيانات الحالية وكل خصائص لوحة التحكم.
- حسابات المدير والموظفين وكلمات المرور.
- القوالب والتصنيفات والعناوين الفرعية ومواعيد المتابعة.
- جهات الاتصال وسجل العمليات والتذاكر والمحادثات المحفوظة في المشروع.

### ملاحظات مهمة

- استيراد `chat_id` لا ينقل تاريخ الرسائل الموجود داخل Telegram أو داخل النظام السابق للبوت الجديد؛ يبدأ المشروع بتسجيل الرسائل الجديدة بعد تحويل Webhook.
- الطلاب الذين تواصلوا مع البوت الجديد سابقًا يمكن مراسلتهم منه. أما جهة اتصال موجودة في قاعدة البيانات القديمة ولم تبدأ البوت الجديد، فقد يرفض Telegram إرسال الرسالة إليها.
- عند الإرسال إلى «كل جهات الاتصال» بعد النقل يجب التأكد أن الجمهور تابع للبوت الجديد، حتى لا تُرسل محاولات إلى مستخدمي البوت القديم فقط.
- تغيير Token يفصل المشروع عن البوت الحالي، لكنه لا يحذف قاعدة البيانات أو السجلات القديمة.
- لا يُلغى البوت القديم نهائيًا قبل نجاح اختبار الإرسال والاستقبال على البوت الجديد.

---

## 24. ربط منصة طفرة وصفحة الطلاب

- اتصال مشفّر بمنصة طفرة مخصص للقراءة فقط، ولا يحتوي على عمليات تعديل أو حذف لبيانات المنصة.
- حفظ نسخة محلية من بيانات الطلاب وربط أصحاب أرقام محادثات تيليجرام بجهات الاتصال دون حذف المحادثات أو التصنيفات أو الموظف المسؤول.
- صفحة مستقلة باسم «طلاب المنصة» متاحة للمدير.
- البحث بالاسم أو الهاتف أو هاتف ولي الأمر أو كود الطالب أو اسم مستخدم تيليجرام أو رقم المحادثة.
- فلاتر حسب الصف، ونوع التعليم «عام أو أزهري»، وحالة الحساب، وحالة ربط تيليجرام.
- فلتر «الموظف» حسب الموظف المسؤول عن تذكرة الطالب في صندوق الدعم. خيار «غير مسند» يشمل الطالب الذي لديه تذكرة لم تُسند لأحد، والطالب الذي لا تذكرة له أصلًا؛ فكلاهما بلا موظف مسؤول.
- زر «فتح الشات» أسفل اسم كل طالب في نتائج البحث والفلترة، ينقل مباشرة إلى محادثته في صندوق الدعم. ويظهر مكانه «لا يوجد شات» للطالب الذي لم يتواصل مع البوت بعد.
- إسناد جماعي: بعد الفلترة والتحديد يمكن إسناد الطلاب المحددين لموظف واحد دفعة واحدة، بنفس أزرار التحديد المستخدمة للمراسلة. الطالب صاحب المحادثة القائمة تُنقل تذكرته للموظف، ومن لا تذكرة له تُنشأ له واحدة. ويُستثنى من لم يبدأ محادثة مع البوت أصلًا لأنه بلا شات يُسنَد، ويظهر عددهم في رسالة التأكيد.
- تقسيم النتائج إلى صفحات لسرعة العرض على الحاسوب والهاتف.
- عرض حالة المزامنة وعدد الطلاب والطلاب المرتبطين بتيليجرام داخل الإعدادات.
- المحافظة وتاريخ الاشتراك غير موجودين في قائمة الطلاب المختصرة؛ تحتاج المحافظة إلى مزامنة تفاصيل الطلاب، بينما يلزم تحديد مصدر موثوق لتاريخ الاشتراك قبل إضافته.
