# CLAUDE.md

**اقرأ سكيل `codebase-map` قبل أي تعديل على الكود في المستودع ده** (`.claude/skills/codebase-map/SKILL.md`). فيها الستاك، بنية الملفات، نموذج البيانات، أعراف الكود، مصائد الإنتاج، وطريقة النشر والتحقق — تُغني عن استكشاف المشروع من الصفر.

## 🚦 النشر والدفع مباشرة — بدون استئذان

**موافقة دائمة على النشر و`git push`.** بعد التعديل والتحقق والكوميت، كمّل على طول من غير سؤال: انسخ الملفات لـ `/root/app`، شغّل `npm run migrate` لو `schema.sql` اتغيّر، اعمل `systemctl restart telegram-broadcast-manager`، تحقق، وادفع لـ origin. بلّغ بالنتيجة بعد الفعل.

قبل الدفع افحص الدِف الخارج من أي أسرار (IP، مفتاح، باسورد، معرّفات تانل) — الريبو public. ده تدقيق مش استئذان.

الاستثناء الوحيد: تعديل schema **مدمّر أو غير قابل للرجوع** (`DROP TABLE` / `DROP COLUMN`، أو `UPDATE`/`DELETE` بيضيّع بيانات إنتاج) — يتعرض قبل التنفيذ. إضافة أعمدة وفهارس بـ `IF NOT EXISTS` نشر عادي مش محتاج سؤال.

## مصائد الإنتاج

تلات حاجات مكتوبة هنا كمان لأنها بتوقّع الإنتاج لو حصلت بالغلط:

1. **متحطّش رابط عام في `.env` المحلي.** تليجرام بيسمح بـ webhook واحد للبوت، فتسجيل رابط محلي **يسحب البوت من سيرفر الإنتاج** ويوقف استقبال الرسايل على `habashy-follow-up.com`. `PUBLIC_URL` محليًا مسيّب فاضي عن قصد.

2. **الإنتاج (`/root/app`) مش git repository.** النشر نسخ ملفات يدوي، والكود المنشور ممكن يسبق اللي متكوميت. `git log` مش دليل على إيه المنشور — التحقق الوحيد الموثوق مقارنة checksums.

3. **الريبو public.** بيانات السيرفر وقاعدة البيانات في `ACCOUNTS.md` المتجاهل في `.gitignore`. متكتبش أي IP أو مفتاح أو باسورد في ملف متتبَّع.
## ⚠️ التحقق: `node --check` مش كفاية

تلاتة عدّوا من الفحص النحوي ووقعوا وقت التشغيل:

1. **متغيّر مش موجود** — سطر بيستخدم `panel` وكل اللي حواليه بيستخدم `list`. صحيح نحويًا،
   بيرمي `ReferenceError` وقت التشغيل، ووقّف صفحة الموظفين بالكامل.
2. **أعمدة `UNION` مش مترتبة زي بعض** — عمود اتضاف في مكان مختلف في الفرعين. الاستعلام كان
   هيفشل لكل التذاكر.
3. **نداء دالة مش موجودة في الوحدة** — `events.emit(...)` و`events` مابيصدّرش `emit` أصلًا.
   الرسالة الصوتية كانت بتوصل الطالب فعلًا، وبعدين السطر ده يرمي، والكاتش يمسح الملف ويرجّع
   خطأ للموظف. **اتستخدمت الميزة مرة واحدة وفشلت الفشل ده.**
   قبل ما تنادي `x.y()` على وحدة محلية، اطبع `module.exports` بتاعها وشوف `y` موجودة.
4. **الكتابة فوق ملف موجود.** `src/utils/phone.js` كان فيه `toEgyptianMobileE164` واتكتب فوقه
   ملف جديد فيه دوال تانية — صفحة إسناد المكالمات وقعت بـ "is not a function". **اقرا أي ملف
   قبل ما تكتبه حتى لو فاكر إنك بتعمله جديد**، والأسماء العامة زي `phone.js` و`utils.js`
   أرجح حاجة تكون متاخدة بالفعل.

**قبل نشر أي تعديل على `dashboard.ejs`:** استخرج السكريبت وافحصه:
```bash
node -e "const s=require('fs').readFileSync('src/views/dashboard.ejs','utf8');
require('fs').writeFileSync('/tmp/s.js',[...s.matchAll(/<script(?![^>]*src=)[^>]*>([\s\S]*?)<\/script>/g)]
.map(m=>m[1]).join('\n').replace(/<%[-=]\s*[\s\S]*?%>/g,'null').replace(/<%[\s\S]*?%>/g,''));"
node --check /tmp/s.js
```
وابحث عن المتغيّر اللي استخدمته في الدالة اللي حواليه — الأسماء بتختلف بين الدوال.

**وقبل نشر أي تعديل على استعلام فيه `UNION`:** اطبع أسماء الأعمدة من الفرعين وقارنهم واحد
واحد. الاختلاف في الترتيب بيرمي خطأ نوع، والاختلاف في العدد بيرمي خطأ صريح — الاتنين بيقعوا
وقت التشغيل مش وقت الفحص.

## ⚠️ منصة طفرة: جلسة واحدة بس لكل حساب

اتقاست: تسجيل دخول تاني بنفس البيانات **بيقتل التوكن الأول فورًا**. وده كان سبب وقوع مزامنة
الطلاب لشهور برسالة "يجب أن تكون مسجل الدخول" من غير سبب ظاهر.

**متعملش `new TafraReadOnlyClient` أبدًا** — استخدم `getSharedClient(credentials)` من
`integrations/tafraSession.js`. الاستثناء الوحيد التحقق من بيانات دخول جديدة في `saveCredentials`.

اللي كان بيقتل المزامنة: مزامنة الاختبارات (كل ١٥ دقيقة)، توزيع المكالمات (كل ١٠)،
و**أي موظف بيفتح كارت تقرير طالب** (`lessonViews` بيجيب لحظيًا من المنصة).

ومتشغلش مزامنة من عملية node منفصلة — عملية تانية = جلسة تانية = قتل جلسة التطبيق.
صفّر `tafra_sync_status.completed_at` وسيب جدولة التطبيق تلتقطها.

## التحقق على بيانات الإنتاج — قراءة فقط

الفحص الأنفع في المشروع ده تشغيل الاستعلام الجديد على الإنتاج قبل النشر. للكتابة استخدم
`BEGIN; ... ROLLBACK;` — بتشوف عدد الصفوف اللي هتتأثر من غير ما تغيّر حاجة. اتكشفت بيه فلاتر
مكسورة وأعداد غلط أكتر من مرة.
